# 评测方案与结果报告

> 报告生成日期：2026-08-29。所有数字可由仓库内命令与数据文件复现。

## 1. 评测方案

### 1.1 案例集

- **60 个固定案例**（`data/evaluation/gold-cases.json`，由 `src/learning/evaluation.ts` 确定性生成）：三画像（foundation / advanced / maintenance）各 20，覆盖 AI4I 与 MetroPT-3 两个数据集、六类资源、基础/进阶/迁移三层任务。
- 三组差异化画像先验：`data/evaluation/personas.json`（与 `pnpm demo:seed` 的诊断作答模式一致）。
- 黄金知识点 → 检索关键词映射：`data/evaluation/gold-knowledge-points.json`。

### 1.2 三项官方指标口径（固定，见 `server/runs/metrics.ts`）

| 指标 | 口径 | 阈值 |
|---|---|---|
| 幻觉率 | unsupported 事实 Claim / 全部可审计事实 Claim；`non_factual` 不入分母；空分母报 N/A | < 5% |
| 难度适配准确率 | 目标区间 ∩ 脚手架 ∩ 画像全匹配案例 / 全部案例（预测与实际作答分开报告） | ≥ 85% |
| 核心知识覆盖率 | 有有效 evidence edge 支持的必备知识点 / 黄金必备知识点（live 按声明证据边判定，非关键词命中） | ≥ 90% |

### 1.3 结果分层（诚实口径）

报告区分四层：**离线规则结果**（`pnpm evaluate` 默认）、**存量真实运行结果**（数据库历史运行）、**本次 live 结果**（`--live`，需用户授权后运行）、**人工复核**。未跑全量 live 时如实标注 `stratified_live_N + offline_full_60`，不笼统称全量实测。

## 2. 当前结果

### 2.1 离线规则结果（`pnpm evaluate`，60 案例全量）

- 难度适配准确率 **100%**（60/60，预测口径）
- 核心知识覆盖率 **100%**（黄金知识点全部有知识库支撑）
- 幻觉率：离线模式不调用模型，**N/A**（需要 live 运行产生声明数据；不以 0 冒充）
- 阈值校验通过；报告：`data/evaluation-report-*.json`

### 2.2 存量真实运行结果（消融实验 `pnpm ablation`）

| 组 | 结论 | 结果 |
|---|---|---|
| A1 动态 DAG vs 固定 DAG | 三画像计划真实分化 | 动态 **3 种计划**（固定仅 1 种）✔ |
| A2 门禁消融（初稿 vs 终稿，按轮次口径） | 已发布运行终稿幻觉率 | 分析 20 次已发布运行，终稿 unsupported 事实声明 **0**（< 5% 达标）✔ |
| A3 BKT 难度校准 vs 固定难度 0.42 | 校准预计成功率入 65–80% 教学区间 | **33/33**（固定难度仅 2/33）✔ |

- 门禁净增益（实测，三画像提交数据包中的 maintenance 案例）：初稿幻觉率 **25.0% → 终稿 0.0%**，门禁净增益 **25 个百分点**；`pnpm replay:run` 离线回放三轮与在线裁决完全一致。foundation / advanced 两包同样通过全部八项完整性校验（含 2 轮修订 + 发布放行）。

### 2.3 离线复核能力（第三方可复现）

- 导出包完整性七项校验（产物散列、DAG 闭合、事件 seq、inputRefs、Claim—Evidence、裁决只紧不松、发布 fail-closed、敏感字段）全部有单元测试；
- 注入式验收已验证：篡改产物 → 散列校验发现；删除证据边 → 完整性发现；放松裁决 → 回放发现（`server/runs/metrics.test.ts` 13 用例）。

### 2.4 live 评测状态（如实说明）

- 已有历史分层 live 评测（9 个分层案例，`data/` 下历史报告）验证真实生成链路；
- 60 案例全量 live 需要模型调用量授权，执行方式：`pnpm evaluate --live --stride N`（分层），报告自动区分口径。**当前提交不宣称未执行的 live 全量结果。**

## 3. 人工复核方案

12 个分层样本 + 全部失败案例人工复核，每样本回答：是否存在事实错误 / 证据是否真正支持 / 引用能否定位 / 难度是否适合画像 / 必备知识点是否讲到 / 行业边界是否清楚 / 下一步动作是否合理。复核表随演示包归档。

## 4. 自动化测试基线

- **23 个测试文件 / 163 个用例**全部通过；根 TypeScript 0 错误；前端 tsc 与生产构建通过。
- 覆盖：协议编排、产物散列、策略修正、声明分类与核验、决策纯函数、指标口径、完整性/回放、账号隔离、路径建议等核心模块。
