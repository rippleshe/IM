# 项目阅读发现

> 持续记录代码事实、产品含义和待确认问题。

## 代码事实

### 1. 当前前端确实是一个完整的协同工作台

- 唯一业务页面是 `web/src/app/page.tsx`，采用左侧智能体与历史会话、中间聊天与输入、右侧计划/工作流/任务/统计/报告三栏结构。
- 输入后会创建会话，连接 WebSocket，再按“澄清 → 复杂度分析 → direct / collaboration / deep / workflow”分流。
- 页面实时展示规划完成、Agent 思考/响应、工具调用、任务状态、评估结果和最终报告；这部分不是静态动画。
- 右侧“任务”目前展示的是一次协同执行的子任务，不是学习路径任务。
- 当前模式选择器是“自动、深度协同、工作流、串行协作、并行协作、专家组”，它表达的是系统执行方式，不是用户要生成的学习资产类型。
- 输入框已经使用“学习或训练需求”的文案，但没有讲义、练习、测评、实训、学习计划等输出意图字段。

### 2. 后端持久化对象仍然是“执行会话”

- `PersistedSessionSnapshot` 只保存 `id / status / mode / task / plan / result / workflowResult / error`。
- 深度执行结果保存最终文本、Token、耗时、评估分数、迭代次数和子任务进度。
- 没有 learner、knowledge point、learning evidence、learning asset、path node、assessment attempt 等业务对象。
- `Memory` 和 `EnhancedSharedMemory` 主要是单次会话/单次集群内存，服务于 Agent 上下文和任务结果，不是跨会话的学习者长期状态。

### 3. 现有“评估”评估的是内容交付质量，不是学习效果

- `DeepEvaluator` 的四个维度是 accuracy、completeness、consistency、format。
- 规则和提示词偏向长报告：目标字数、章节数、数据支撑、引用、格式。
- 这套评估很适合做生成内容审核的基础，但不能直接代表答题正确率、知识点掌握度、资源难度适配度或路径调整依据。
- 普通 collaboration 路径甚至将 `evaluationScore` 固定为 0.8，说明目前这条路径的质量分不是学习闭环中的可靠证据。

### 4. 领域知识接入目前还是占位层

- `knowledge_base` 工具会返回“需要具体数据源接入”的查询框架，不会检索真实专业文档。
- 当前工具集合偏通用研究/写作：搜索、抓取、数据分析、代码执行、报告写作、知识库、计算器。
- 因此“资源中心”的真实价值暂时无法成立：生成结果没有稳定的知识点映射、来源、审核证据和后续学习关系。

## 产品判断

### 核心定位

保留现有页面，正式把它定义为“智能协同工作台”。但它的任务输入需要从自由文本升级为：

```text
学习目标 + 输出意图 + 学习者上下文 + 约束
```

其中输出意图至少包括讲义、练习、测评、实训、学习计划五类；它应当进入任务规范和 Agent 计划，而不是仅仅拼到 Prompt 前面。

### 四类业务对象

```text
学习资产 = 系统产出的可复用内容
学习证据 = 学习者实际表现留下的事件
学习者状态 = 根据证据更新的知识点估计
学习路径 = 基于目标、依赖和状态维护的可执行任务图
```

它们的最小关系是：

```text
工作台生成资产 → 学习者使用/作答 → 产生证据
证据 → 更新知识点状态 → 重排路径
路径变化 → 再次驱动工作台生成下一份资产
```

### 页面优先级

- 第一优先：协同工作台，增加输出意图和“生成后保存为学习资产”的动作。
- 第二优先：路径/今日任务视图。它比完整资源中心和雷达画像更能证明动态闭环。
- 第三优先：资源工作区，按“继续学习、关联目标、关联知识点、学习结果”组织资产，而不是按资源类型做画廊。
- 第四优先：画像详情和测评历史。画像先作为证据驱动的侧边详情/报告，不宜先做独立雷达图页面；测评先作为路径更新事件，不宜先做孤立大页面。

### 最小闭环建议

先只选一个垂直场景和一组小知识图谱，例如“卡尔曼滤波”或现有演示中的一个工程知识主题。先实现：

1. 选择“生成练习”并带出知识点、目标难度、预计时长。
2. 生成结构化练习资产：题目、选项/输入、答案、解析、知识点、难度、来源/审核状态。
3. 用户提交答案，记录 evidence，而不是只把答案拼回聊天文本。
4. 根据证据更新单个知识点的掌握度、置信度和趋势。
5. 路径节点根据阈值变为继续、补强、解锁或跳过，并给出下一步生成动作。

这条链跑通后，再扩展讲义、实训和多知识点路径。否则先做四个页面只会把“没有业务状态”包装成四个展示页。

## 需要警惕的实现问题

- 现有深度计划默认长报告（30000 字、8 章节、引用/数据），与讲义、练习、测评等学习资产的输出契约不匹配，需要按 asset type 改成结构化 schema。
- `executeCollaborationWithConfig` 在前端用 `allAgentPool.slice(0, agentCount)` 选 Agent，角色顺序不是学习任务语义驱动；生成练习时应明确诊断、领域知识、题目生成、审核、难度适配等职责。
- 现有输出下载文件名仍有 `pi-report` 旧品牌痕迹，属于产品收口时应处理的非核心问题。
- 页面中仍有较多英文状态与通用报告措辞，当前不影响协同底座，但会削弱学习产品叙事。

## 垂直领域与 CSV / RAG 建议

### 推荐方向

建议把主场景收敛为：

> **智能制造设备故障诊断与实训**

理由是它同时具备：结构化 CSV 工况/传感器数据、设备手册与 SOP 等权威文档、可生成的案例题/实训任务、明确的正确性校验，以及较强的挑战杯展示价值。

不要把“CSV 数据分析”作为产品领域。CSV 只是证据载体，真正的领域应是一个需要学习者完成判断和操作的工程任务。

### 数据分工

- CSV：设备、时间、工况、传感器值、告警、故障标签、维修结果等结构化事实。进入 SQLite / DuckDB / Parquet 等可查询层，不把大量行直接切块后全部向量化。
- 文档：设备手册、维修 SOP、安全规范、术语说明、故障处置流程。切块后做向量/关键词混合检索，并保留文档、章节、页码等来源信息。
- 案例：由 CSV 行或时间窗口生成的可复现实训案例，绑定原始行 ID、故障标签、相关知识点和标准处置步骤。

### RAG / 幻觉控制

查询先分类：数值/趋势/筛选走结构化查询，规范/原理/步骤走文档检索，复杂问题走混合检索与重排。生成 Agent 只接收带来源的证据包，输出必须带证据引用；证据不足时必须拒答或追问。

审核 Agent 不只看语言质量，而要逐条检查：事实是否能在证据中找到、数字和单位是否一致、操作步骤是否违反 SOP、故障结论是否超出数据支持范围。数值、阈值、单位和公式优先使用确定性校验器，不交给 LLM 自由判断。

### 匹配度如何做

资源匹配度不应由模型直接给一个“看起来合理的百分比”。先建立可解释评分：

```text
匹配度 = 知识点适配 + 难度适配 + 数据复杂度适配 + 目标适配 + 时间适配
```

学习者答题正确率、提示次数、完成时长、步骤错误位置等形成 evidence，再更新知识点掌握度和置信度。资源难度适配率应通过“系统推荐难度”和“学习者实际表现”对照评估，而不是只看模型自评。

### RAG 边界与数据责任（2026-08-26）

结合 `IM-Training-Agent-datasets` 和用户提供的“六大资源”方案，首个可演示垂直场景收敛为：

> 工业空气压缩机故障诊断与运维训练。

主数据使用 MetroPT-3：约 151.7 万行、15 个传感器/状态字段、带时间序列和空气泄漏故障窗口；AI4I 仅作为小型、可解释的规则与测评基准（1 万行、339 条故障记录），不作为主要工业叙事。液压数据保留为后续扩展，不在第一阶段接入。

RAG 不做“所有 CSV 行向量化”。

- MetroPT-3 原始 CSV 进入结构化查询层；当前实现先用 SQLite 查询字段、时间窗口、趋势、样本和故障区间，后续数据量或性能需要时再评估 DuckDB。
- 官方说明、字段字典、故障处置知识进入文本检索；第一阶段先 SQLite FTS5/关键词，稳定后只对文档块增加向量检索。
- SQL 结果、文档片段和学习者状态合并成 EvidencePack；生成 Agent 只能依据 EvidencePack 生成 ResourceDocument，审核 Agent 逐 Claim 检查数字、单位、定义和操作建议。
- 画像更新只读学习证据（答题、提交、完成、提示），不读普通聊天，也不读用户上传文件来改变能力值。

数据责任分界：

- 系统预置：数据集、字段字典、知识点、审核规则、案例窗口、少量评测题和演示画像。挑战杯演示不能依赖用户先上传资料。
- 用户上传：默认是本次对话的临时参考材料；按需读取表头/片段或文档片段，低信任、只影响当前回答，不进入公共知识库、不写入知识图谱、不自动更新画像。只有用户明确“保存为我的资料”时，才另存为个人资料，并再次确认是否纳入检索。

现有代码的落差：学习业务和 MetroPT-3 结构化查询已经有 SQLite 与 EvidencePack 最小闭环，但答题事件驱动技能状态、Claim 级审核和完整的 Agent 运行关联仍待补齐。因此下一步不是做六个页面，而是跑通：工作台提问 → SQL/文档混合检索 → EvidencePack → 结构化资源 → Claim 审核 → 保存学习资产 → 答题反馈更新路径。

### 页面收敛

保留当前工作台作为唯一高频入口。右侧继续放执行视图 Plan / Flow / Tasks / Stats / Report；学习视图只设一个“学习中枢”二级入口，内部使用轻量标签切换“路径 / 画像 / 学习资产”，不再增加多个一级页面。资源中心只列出会话产生的讲义、练习、测评、实训等资产，详情再打开对应 Renderer；不做资源关系图作为首屏。

### 当前落地校正（2026-08-26）

前期闭环已先按可靠性落地为 SQLite，而不是引入额外的 DuckDB 运行依赖：MetroPT-3 的结构化查询进入 `datasets.sqlite`，学习业务进入 `learning.sqlite`，文档检索使用 SQLite FTS5。一次真实学习任务会调用模型生成路径节点，然后用同一个任务的 EvidencePack 保存讲义、分层练习、知识图谱三类学习资产，右侧“路径 / 资产”直接读取这些持久化结果。

用户上传文件目前只作为本次输入的临时参考，前端读取文本类文件并随任务发送，不导入 RAG、不改变画像、不进入公共资产库。画像的资产数、今日资产数、学习时间和正确率由 SQLite 事件/资产计算；关键词和雷达图由用户点击“重新生成画像”时调用模型生成并保存快照。答题事件和 Claim 级审核仍是下一阶段，不应提前伪装成已完成能力。

## 挑战杯对齐后的协同设计结论（2026-08-26）

挑战杯真正要求的是一条可验证的闭环：画像/先验 → 任务理解 → 证据检索 → 个性化资源 → 专业审核 → 学习反馈 → 路径与画像更新。提供的 Agent 设计已经覆盖职责，因此不再继续堆角色，而是统一每个 Agent 的输入输出协议。

### 1. Agent 的稳定职责

- 学情诊断：根据学习证据估计知识点掌握度、置信度、风险和资源适配建议；不读取普通聊天来直接改能力值。
- 任务理解：把用户目标转成领域、知识点、技能任务、输出意图和完成标准。
- 知识检索与溯源：结构化数据走 SQL/时间窗口，领域文档走 FTS/向量检索，统一生成带定位、信任级别和覆盖率的 EvidencePack。
- 资源生成：只接收任务规范、学习状态和 EvidencePack，生成讲义、分层习题、实训指南、知识图谱等结构化资产。
- 审核与交叉验证：逐 Claim 检查来源、数字、单位、阈值、步骤和难度；证据不足时改写为不确定、补充检索或拒绝发布。
- 反馈与路径决策：把答题、提示、完成、耗时等学习证据转成状态变化，决定补强、保持、解锁、跳过或生成挑战。

SQL 查询、指标统计、匹配度计算、路径状态更新和隐私过滤属于确定性服务，不让模型自由编造。辩论 Agent 是审核阶段的协同机制，不应成为脱离证据的“多模型聊天”。

## 产品蓝图收敛（2026-08-27）

产品首期不再泛称“学习助手”，而是：**面向工业设备运维初学者与一线技术人员的空压机状态监测与故障诊断训练智能体**。学生/职校与本科自动化方向学习者是首要演示对象；企业新入职运维人员是直接迁移对象；教师/培训师是后续组织端用户。

首次进入只做一次 2 分钟的起点诊断：角色与目标、已有经验、每周时间、5 题自适应基础题。其结果是带来源的 ProfileEvidence，不在每次聊天重新提四个问题。聊天入口只接收当前任务；如果任务不能判断目标，只补问一个与当前目标直接相关的问题。

空白中间栏不放通用宣传文案，而应读取 `next_action`：已有路径时展示当前节点和“学习 / 做 5 题 / 分析案例”三个动作；首次诊断完成但没有路径时展示“生成首条路径”；无资料且未诊断时才引导完成起点诊断。它们均来自当前 learnerId 和 goalId，而不是硬编码卡片。

六个智能体是角色池而非固定队列：顶层编排 Agent 把任务转为任务 DAG，可按需派发多个检索、诊断或资源生成实例；顺序/并行/专家组/深度协同是内部策略，用户侧只保留“自动、快速答疑、深度研判”三种意图。交叉验证和隐私合规是发布门禁，不是可关闭的独立模式。

数据库采用混合建模：SQLite 保存用户、学习路径、作答、资产、Run/Task/Event、证据和 FTS5 文档索引；关系和状态使用列与外键，资源块、题目、知识图谱、Agent 契约使用版本化 JSON。原始工业时序后续转换为 Parquet 并由 DuckDB 分析；当前 SQLite 数据集查询可继续作为演示闭环。原始 CSV 永不做向量化；文档做 FTS5，文档规模扩大后再补向量召回。

### 2. 六种顶部模式的定位

六种模式是同一套 Agent 的编排策略，不是六套产品，也不是六种资源类型。

- 自动：路由器根据目标复杂度、证据风险、输出类型选择直接回答、串行、并行、专家组或深度协同，并把选择原因写入 Plan。
- 深度协同：完整跑通诊断、检索、生成、审核、路径更新，适合生成多种学习资产或处理高风险专业问题。
- 工作流：把本次任务展开为可观察、可调整的阶段图；它是“当前任务怎么执行”，不是学习者长期路径。
- 串行协作：有依赖的流水线，例如任务理解 → 检索 → 生成 → 审核 → 发布。
- 并行协作：互不依赖的领域分析、学情分析、资料检索并行，最后由汇总/审核角色合并。
- 专家组：多个专业角色对关键判断给出证据、风险和意见，裁判角色形成通过、修改、补检索或拒绝的决策。

每个模式都应由配置描述 `agentRoles / executionGraph / outputContract / auditPolicy / rightPanelBindings`，前端不再靠 Agent 列表顺序或硬编码切片来决定职责。

### 3. 右栏必须成为同一 Run 的投影

- 计划：本次 Agent 计划和当前学习目标，不是静态示例。
- 流程：实际发生的阶段、分支、重试和审核回退。
- 任务：每个 Agent 的状态、输入证据、输出摘要、工具调用和耗时；只展示执行记录，不展示模型私密思维链。
- 反馈：审核结果、答题结果、画像/路径变化及变化原因。
- 结果：最终学习资产、引用、审核状态和下一步动作。
- 路径：从同一个学习任务生成并持久化的长期任务图。
- 画像：代码统计指标 + 模型根据证据生成的关键词、摘要、雷达图快照。
- 资产：本次及历史会话生成并保存的讲义、分层习题、实训、知识图谱。
- 证据：本次 EvidencePack、数据行/时间窗口、文档定位和 Claim 关联。

因此，右栏不是另一个演示区；它们都通过 `runId / learnerId / evidenceIds / assetIds / pathItemIds` 关联到同一次任务。

### 4. 前期数据责任边界

系统预置 MetroPT-3、字段字典、领域说明、故障/知识点定义、审核规则和三组差异化演示画像，保证挑战杯演示不依赖用户先上传资料。用户上传资料默认只作为本次任务的低信任临时参考，不进入公共 RAG、不自动改变画像、不直接写入学习资产；只有用户明确保存时才另建个人资料来源。

前期继续采用 SQLite：`datasets.sqlite` 存数据集、字段和文档索引，`learning.sqlite` 存学习资产、证据事件、技能状态、路径、画像快照和 Agent 运行关联。先把 SQLite SQL + FTS5 的可解释闭环跑通，再考虑向量检索，不把原始 CSV 行直接向量化。

### 5. 可靠执行顺序

先固定任务上下文和 Agent 事件协议，再把六种模式接入同一套编排配置；随后完成“生成分层习题 → 用户作答 → 记录学习证据 → 更新技能状态 → 右栏路径变化”的单条闭环；最后补 Claim 级审核、知识图谱渲染、三组画像对比和指标测试。当前不新增独立大页面，也不把模型生成的自由文本直接当成路径或画像数据。

## 垂直领域重定向：工业数据诊断工具开发训练（2026-08-27）

用户不应从数百万行工业时序数据开始学习。因此首个场景从“直接培训工业诊断”调整为：

> **面向工业设备数据的 Python 数据分析与诊断应用开发训练。**

最终实训项目是开发一个本地诊断工具：读取压缩机传感器数据、执行查询/统计、解释异常窗口并生成带引用的诊断报告。MetroPT-3 是中后期真实案例，不是入口课程。

### 四层资料库

1. **基础知识库**：Python、CSV、SQLite、Pandas、可视化与异常分析的版本化官方文档片段和字段术语卡。
2. **可执行练习库**：小型合成或脱敏 CSV、预置代码骨架、SQL/代码题及可运行的标准检查器。
3. **真实案例库**：按任务切出的 MetroPT-3 时间窗、字段说明、故障报告及可复现实验条件；原始全量 CSV 不进入 RAG。
4. **评测金标准**：三类画像、标准答案、预期代码输出、证据定位及人工审核结论，用于衡量资源适配、覆盖率和 Claim 可证性。

RAG 只面向第 1、3 层的可信文本；表格与时序问题经 SQLite/DuckDB 工具查询。用户上传文件保持会话级低信任参考，除非用户显式保存，否则不进入系统知识库、不更新画像。

### 最小交付闭环

```text
注册/登录 → 起点测评 → 学习目标 → 路径节点
→ 讲义 / 代码练习 / 小案例 → 提交运行或作答结果
→ 学习证据 → 技能状态与路径更新 → 下一项行动
```

每一次资源发布都必须经过“检索证据 → Claim 审核 → 交叉验证门禁”。六个角色根据任务 DAG 按需实例化，而不是每次按固定顺序全部运行。

## 工作台线框收敛（2026-08-27）

用户确认采用“一套主体工作台、三个一级工作状态”的信息架构，而不是把画像、路径、资源拆成很多独立大页面：

- **路径**：左侧为用户可编辑的学习路径，中心展示当前节点的说明/建议，右侧展示动态学习画像与路径建议。
- **学习**：中心是与当前节点关联的任务对话和实训内容，右侧显示当前路径；聊天不再反复弹出通用四问表单。
- **资源**：左侧仅列当前节点或历史生成的讲义、习题、PPT/演示稿、知识图谱、问答；中心按资产类型渲染，右侧按情境显示笔记、作答反馈或知识溯源。

讲义使用 Markdown 富文本作为正文模板，但必须有资产元数据（知识点、证据 ID、审核状态、来源 Run、创建时间）；习题以 JSON 保存交互题干/答案/判分规则，Markdown 保存解析；知识图谱保存 Mermaid 源和节点关系 JSON；问答消息逐条关联证据来源。

路径节点不由系统自动“完成”。节点的 `user_status` 只能由用户手动设置为未开始/进行中/已完成；系统从作答、阅读、运行结果等证据计算 `recommendation`（保持、补强、可进阶），用户可采纳或忽略。这样既保留学习者自主性，也能满足挑战杯对动态反馈的要求。

挑战杯证据不展示私密思维链，而展示可核验的 `EvidenceCard`：来源标题、类型、精确定位（文档章节/页码或数据集时间窗/行范围）、对应 Claim、审核结论。讲义可内联引用；习题答案在提交后显示关联依据；问答固定在右侧“知识溯源”面板展示。
