[ERROR] - (starship::print): Under a 'dumb' terminal (TERM=dumb).

# 项目决策记录（精简版）

## 真实写入功能验收（2026-09-03）

- 基础学习者账号已实际保存一条关于“单点数据不能直接证明设备稳定”的学习笔记；资源问答返回了时间连续性、多变量和现场背景等可用依据，并明确了证据边界。
- 已完成习题的再次作答、关键要点自评和资源“掌握一般”反馈；路径节点撤销掌握后再恢复，状态可准确回写。
- 完成一次完整真实协同运行 `study-run-ea491a98-562c-4517-b733-cbb4dddc2067`：首稿的 4 条无依据声明被门禁拦截并退回；第二轮 `76/76` 条事实声明有依据，随后通过质询、裁决和隐私检查后才发布。
- 资源库可实际读回通过成品 `PPT · 看懂数据，找准问题`（15 页），正文覆盖“现象—数据—判断—下一步”并明确“不能凭一点数据直接下故障结论”。本次接口的资源类型为用户在选择器中明确选定的 `presentation`；任务文字中的“讲义”不会暗中覆盖该选择。
- 跨页面后路径仍同时保留“已掌握”与“待开始 / 继续学习”状态，未出现假结束横幅；页面控制台和服务错误日志均无错误。`pnpm test` 27 个文件、204 项检查全部通过，Web :3000 与 API :3001 保持监听。

## 发布前真实产品路径回归（2026-09-03）

- 以已登录演示账号手工完成路径节点切换、学习状态恢复、讲义/PPT/习题/知识脉络阅读、笔记与问答输入、习题再次作答、验证记录展开，以及模型/任务分工设置加载；全程无页面控制台错误。
- 学习页展示的是服务端已完成运行时，`10 / 10`、完成提示和公开摘要一致；跨工作台返回后状态保持，不会由页面切换改变。
- PPT 翻页、进入放映和显式“退出放映”均正常。自动化环境不能将系统级 Esc 交给全屏页面，不作为产品缺陷处理。
- 设置页的真实模型连接已成功返回：当前执行路由为 `opencode-go / mimo-v2.5`，未出现超时或前端错误；未发起会污染演示账号的资源生成任务。
- 既有回归套件 `pnpm test`：27 个文件、204 项检查全部通过。未新增测试代码，未提交学习者作答、笔记或模型请求。

## Claude 重复实现审查（2026-09-03）

- `e0afcb0` 新增的画像报告、苏格拉底对话、证据卡和进度增强层与项目已有的画像洞见、持久化导学、验证工作台和协同进度重复。
- 新苏格拉底服务将会话放在进程内 `Map`，服务重启即丢失，也没有既有导学主链的 BKT 更新与资源/路径决策；不能作为产品能力保留。
- `dag-progress-enhanced` 忽略真实的完成、摘要和事件语义，也不支持已有的 `skipped` / `revising` 状态，会再次造成切页后节点错误变绿、任务错误结束。
- 新报告接口读取的字段与当前路径/决策数据模型不一致；新增证据卡未被使用。两者不应以“功能更多”为理由进入发布版本。
- 已采用反向提交保留的方式回撤该批重复实现，不触碰此前已修正的资源体验与进度状态逻辑。

## 学习状态与资源阅读器反馈收口（2026-09-03）

- 学习任务恢复只认 `study_runs.status`：`queued/running` 才代表进行中，`succeeded/failed/cancelled` 才代表终态；未知或未找到运行记录不会被渲染成完成。
- 任务节点只把服务端明确的 `running/succeeded/failed/skipped` 映射到对应语义；跳过显示为“未适用”，排队和取消保持未完成语义，切换工作台不会改变任务本身。
- 学习资源不再显示通用“阅读方法提示”、正文选区引用卡或“讲解词”；PPT 的说明和导出内容统一面向学习者，解释本页新增信息、前后关系、依据和判断边界，不用泛化复述或翻页口令。
- 数据表改为可横向查看的样本卡，误区改为“误区 / 正确理解”对照卡；PPT 放映使用沉浸式全屏画布，翻页导航保持可见，笔记面板继续使用真实保存接口。
- 本轮只做必要的类型检查、生产构建、差异检查、界面规则扫描和浏览器产品路径核对，不新增开发测试逻辑。

> 只保留仍然有效的结论与约定，历史推演过程见 Git 历史。改动架构级设计时才更新本文件。

## 11 条产品反馈闭环（2026-09-03）

- 模型连接失败的直接原因是设置页探测请求本地只等待 20 秒；同一运行配置约 25 秒后可成功返回，因此不是前端缺少 API Key。探测窗口已调整为 45 秒，超时文案会明确指向上游响应慢或排队。
- 学习页的“进行中 / 已结束”现在以 PostgreSQL `study_runs` 中该账号的 `queued/running` 记录为准，不再恢复本地完成快照；切换工作台不会改变运行状态，返回后继续轮询当前任务。
- 路径助手按消息创建时间稳定排序并跟随最新输出，左侧对话与右侧路径之间新增可拖拽分隔条；习题的填空和简答提交后可以再次作答。
- PPT 阅读器增加浏览器放映模式，并将“讲解词”改为面向学习者的“本页说明”；PPT 与知识脉络右侧均使用真实笔记保存面板。
- 本轮只执行类型检查、生产构建、差异检查与浏览器核心交互核对；没有增加测试夹具、测试分支或与产品无关的测试逻辑。

## 失败资源交付语义与阅读密度收口（2026-09-03）

- 发布门禁未通过的草稿不再写入普通学习资源交付链；服务端仅在运行产物、证据、审计和裁决记录中保留追踪，`listAssets` 与 `getAsset` 只向学习者暴露 `passed` 成品。
- 学习页对失败任务统一显示“暂未交付学习材料”，不显示待复核版本、资源查看入口或下载动作；验证页保留处理记录，服务系统负责补充依据、返修和重新生成。
- 资源阅读页移除自动检查警告、创建日期、笔记数量、反馈辅助说明和多余标签行；进一步移除整块资源头部，由左侧目录承担定位，正文从工具栏后直接开始。
- 三组挑战杯演示账号仍保留真实差异化资源与路径；本轮只做产品交付语义与视觉收口，不新增测试夹具或测试分支。

## 三组挑战杯演示账号真实流程核对（2026-09-03）

- 三组账号均可正常登录并读取差异化知识树：基础学习者 15 节、进阶学习者 15 节、在职运维 15 节；资源、路径、验证工作台均从当前登录账号隔离读取。
- 当前资源数量为：基础学习者 26 份（讲义 8、习题 5、PPT 8、知识脉络 4）；进阶学习者 12 份（讲义 5、习题 1、知识脉络 1，另有其他历史资源）；在职运维 6 份（讲义 3、PPT 1，另有历史资源）。
- 在职运维账号实际执行了一次面向现场晨会的 PPT 生成：经历画像分析、数据/资料检索、模型生成、内容审查与三轮返修；最终未发布，完整草稿与裁决依据仅保留在验证记录，不把证据不足的成品冒充已发布资源。
- 验证工作台可以看到该运行的 27 条证据、135 条内容与依据、三轮修订和“未放行”结论；这证明产品门禁在真实账号上生效，而不是只有静态演示数据。
- 本轮只做真实产品流程核对与资源补齐，没有新增单元测试、测试夹具或后端测试分支；侧边栏已打开 `http://localhost:3000/` 供继续查看。

## 挑战杯产品化界面收口（2026-09-03）

- 挑战杯的用户体验评分直接关注流程便捷、协同过程可见和资源排版清晰；当前功能闭环已具备，界面本轮应优先服务这三件事，不扩展新的资源类型或比赛口径。
- 现有工作台沿用浅色工业学习控制台作为视觉基底，新增统一头部、用户身份、工作区图标和轻量“诊断信号”识别；路径空状态改为可直接点击的目标入口，不再留下无意义的大片空白。
- 桌面端验证页从 `max-w-5xl` 放宽到 1320px，路径页在宽屏提高对话区占比；移动端路径、学习、资源改为单列滚动，覆盖原先固定栏宽导致主内容挤压的产品缺陷。
- 后端实际启动链已核对：PostgreSQL / Redis 健康，迁移与数据引导完成，AI4I 与知识卡可用，演示画像幂等种子完成，API :3001、worker 与 Web :3000 均保持运行；本轮不新增测试逻辑。

## 面向初学者的资源教学质量重构（2026-09-02）

- 根因不是单一提示词，而是资源生成链路把字段、学情和证据直接交给模型，缺少统一的教学契约；现在四类资源都要求先解释术语、再给小例子、跟做最小动作，最后说明能下什么结论和不能下什么结论。
- 资源生成同时收到 `resourceType`、当前节点、难度校准、教学模式和动态证据；修订轮保留上一版完整成品，只针对退回声明修改，避免资源类型串台或修订后缩水。
- 资源阅读器增加“先这样读”、术语表、代码前说明、PPT逐页讲解和习题作答反馈；资源正文不再展示原始证据卡，证据定位仍保留在资源与验证链路。
- 发现并修复数据样本串台：代表性表格现在优先使用当前查询命中的结构化行，再按来源聚合；`pandas`、`Python`、`CSV` 等工具词不再决定数据集，兜底代码也跟随所选证据来源。
- 真实回归覆盖四类资源：分层习题完成 9 题并提交简答自评；知识脉络执行了路径与节点小检查门禁；讲义生成了 3000 字以上的完整成品但因 7 条无依据表述保留为待复核；最新 PPT 生成 11 张幻灯片内容（阅读器含封面共 12 页），第 3 轮审核通过并入库，第二页有 3 个白话术语，末页有可直接回答的小练习。
- 本轮 `pnpm test` 为 26 个测试文件 / 203 项用例全部通过；`pnpm typecheck`、根目录 lint、前端 lint、前端生产构建全部通过。3000 前端、3001 后端和 worker 已按最新代码重启并保持运行。

## 文档单一维护源收口（2026-09-02）

- `docs/挑战杯技术开发总规.md` 是当前代码唯一技术基线，已更新为 2026-09-02 的真实状态：四类正式资源、26 个测试文件 / 203 项用例、四个前端工作台，以及当前检索、取消、评测边界。
- 阶段性可信协同升级计划已完成施工使命并移入回收站；其有效内容已归并到技术基线、README、提交包和内部决策/进度记录，源码注释不再依赖它。
- README 只负责仓库入口、运行方式和产品总览；提交包只负责作品介绍、设计实现、测试、评测、部署和提交清单。提交包内数字必须在最终打包前与一次真实回归结果同步。
- 本轮回归：`pnpm test -- --run` 26 个文件 / 203 项通过，`pnpm typecheck`、`pnpm lint`、`pnpm --dir web lint`、`pnpm --dir web build` 全部通过；未运行 live 模型评测、未下载新数据或依赖。

## Ponytail 可信性能收口（2026-09-01）

- 按 Ponytail 阶梯审计后，未新增依赖、未拆分现有核心模块、未增加重试层：现有 BullMQ、PostgreSQL 事务与 SSE 已足够覆盖本轮问题，继续加抽象只会扩大故障面。
- 历史运行统计显示：已发布运行平均约 69 秒；被发布门禁拒绝的运行平均约 484 秒。慢运行主要消耗在最多三轮的“生成—审核—质询—裁决”返修链，而不是队列积压。生成节点是最长单点（平均约 30 秒，P95 约 124 秒），质询与裁决次之。
- 已修复 `appendRunEvent` 的真实并发序号竞争：旧 SQL CTE 不能保证“取最大 seq”位于 advisory lock 之后，多个根节点并行启动时可能撞上 `uq_run_event_seq`。现改为同一 PostgreSQL 事务内先获取 `pg_advisory_xact_lock`、再取号、再插入；24 个并发事件的受控验证得到连续且唯一的 1–24 序号。
- “未入库”不是写库失败的同义词：界面现区分“已发布 / 自动检查未通过、未发布 / 已取消 / 处理失败 / 生成中”。审核未通过的草稿保留追溯链但不会进入资源库。
- 假设性教学示例、数量下界（如“超过 150 万”）和字段类型操作曾被误判为无依据声明，导致不必要的多轮返修；规则已改为先识别教学表达、支持可由精确证据推出的下界，并只在真正的字段语义断言上启用字段字典核验。
- 产品不向普通学习者暴露或转嫁资料审核：服务端智能策展、Claim 门禁与隐私门禁负责是否沉淀。内部 `manual_review_required` 仅是历史兼容状态名，对用户语义为“自动检查未通过，未发布”。

## 挑战杯针对性技术深审（2026-09-01）

- **实时协同并非真实流式**：路径对话原先在服务端等待完整结构化模型结果，再以定时切片模拟输出；接口层却按 JSON 片段解析普通文本，导致 SSE 增量实际被丢弃，页面只能在结束时看到整段答复。现已改为直接使用已有 `MultiModelClient.streamText`，在服务端仅递增解出公开 `reply`；`revision` 必须等完整 JSON 校验后才写路径，既避免暴露内部协议，也避免半段输出修改数据。无流能力提供方只会在模型调用完成后一次性呈现，不再伪造打字效果。
- **资源能力与口径不一致**：`LearningResourceType`、生成器、前端选择器和评测器均只有讲义、PPT、分层习题、知识脉络四类；但早期总规和评测报告曾写“六类”。赛题最低要求已满足，但“实操指南、挑战任务”仍只是任务文案，不是可独立导出、作答或验收的资产。正式评测报告已先改为四类真实口径，下一步应补齐独立资产契约而非继续宣称六类。
- **评测可复现性已收敛**：消融 A2 原先取“最近 20 次已发布运行”，A3 对技能状态 `LIMIT 60` 未排序，日常使用会改变样本和数字。现改为 `data/evaluation/live-run-cohort.json` 固定三份真实画像运行导出，逐份验证 SHA-256、运行标识和离线回放，再按 Claim 数量计算 A2、按起始快照的 33 个技能状态计算 A3。离线 60 案例仍只验证规则与覆盖，不被误写为生成内容幻觉测量。
- **知识库从候选到检索的闭环还差运维层**：策展批准会切分并启用受管文档，检索有全文/向量降级说明；但批准后未确保向量回填完成，用户上传的 `user_declared` 来源也缺少可核验许可证状态。下一步要记录“待嵌入/已嵌入/失败可重试”和许可证证据，不能把全文检索降级误当成完整知识库能力。
- **可靠性与成本边界**：运行总超时目前只拒绝等待，未终止已发出的模型请求；多轮审核失败会继续占用提供方配额。并发量不应先行提高，应先把 AbortSignal、模型调用耗时和限流结果接入已有运行追踪，再按提供方设置上限。
- **提交材料采用单一维护源**：源码 `docs/` 只保留当前代码唯一技术基线；作品介绍、设计方案、部署、测试、评测与提交说明仅维护在根目录提交包，避免两套文档长期漂移。

## 挑战杯材料复核（2026-09-01）

- 已重新执行 60 个离线案例：难度适配 60/60、知识覆盖 60/60；离线模式没有模型声明，幻觉率严格记为 N/A，不能写为 0。
- 已重新执行消融：动态编排在三画像上形成 3 种计划（固定策略仅 1 种）；20 次已发布运行的初稿幻觉率为 6.9%、终稿为 0，5 次触发修订；难度校准 34/34 位于目标区间，固定 0.42 仅 2/34。
- 提交材料必须把“聚合运行结果”和“单个维护画像三轮演示（25% → 0）”分开表述，避免把典型案例误写成全量统计。

## 知识库与网络资料沉淀成熟化（2026-08-31）

- 根目录 `知识库升级方案.md` 已确认为本阶段唯一设计依据：正式知识仅来自审核通过且活跃的受管来源版本；网络搜索结果只作为低可信候选，不得直接生成或污染 `document_chunks`。
- 根目录 `资数据收集料.zip` 已完成只读清点，共 19 份资料：11 份 PDF、8 份 Markdown，主题覆盖工业故障诊断、振动/旋转机械、空压机与车辆气压数据集。当前环境已有 `pdftotext`，无需下载新的解析工具即可完成首轮文本抽取；Docling 保留为后续增强路径。
- 现有 PostgreSQL 已有 `document_chunks`（全文索引 + 1024 维 pgvector）和 RRF 混合检索，但尚无来源、版本、候选审核和导入任务表。升级必须新增这些治理层，同时兼容现有知识卡和 MetroPT-3 的检索结果。
- 本轮资料默认进入候选区：保留原件、哈希、抽取文本、解析质量与许可待核状态；正式切片由服务端智能策展流程决定，不向普通学习者暴露资料库管理员职责。
- 审核晋升必须是事务：先锁定来源和最新版本，删除该版本旧切片，再写入带 `source_version_id`、章节定位、顺序、内容哈希与 token 估算的切片，最后同时将版本设为 `active`、来源设为 `approved`。这样失败时不会出现“来源已启用但切片不完整”。
- 知识库管理权不进入普通学习者身份载荷，也不由前端猜测：资料候选由 `knowledge-curator` 在服务端执行相关性、质量、重复风险与隐私门禁；用户上传默认只保留审计元数据，只有明确同意且策展通过才会生成正式切片。旧 `knowledge_admin` 字段仅作为迁移兼容，不再提供用户侧审核入口。
- 上传正文在隐私节点完成后从 `study_runs.request_json` 抹除；审计查询和清除均按登录学习者隔离，避免用户看到或删除其他人的资料痕迹。

## 模型能力与资源质量决策（2026-08-30）

- 模型的上下文窗口和最大输出属于运行时能力，不再作为用户手工配置项，也不在普通设置界面展示。
- 新增服务商或切换模型时，优先调用其 OpenAI 兼容 `models` 接口同步模型目录，并解析常见能力元数据；接口未返回限制时保留已缓存能力，最终使用内部安全预算，不把某一家模型的参数套给所有服务商。
- 当前 OpenCode Go 的 `models` 响应只返回模型身份信息，没有上下文或输出上限字段，因此“实时读取”必须设计成元数据优先、缓存与安全预算兜底，而不是假装标准接口必然提供所有能力。
- 生成型智能体统一使用角色、目标、输入语义、输出契约、证据纪律和质量自检结构；讲义必须经过教学结构质量门槛，不达标时由模型完整返修一次。
- 资源生成输入必须包含用户原始任务、当前路径节点、学情分析和难度校准；只传写作要求与证据会丢失教学目标，是当前内容泛化和混乱的主要原因之一。

## 数据源收敛决策（2026-08-31）

- 比赛与日常主线统一使用 PostgreSQL 16 + pgvector，Redis 负责队列、锁和运行事件分发。
- PostgreSQL 已实测承载 MetroPT-3 全量数据，因此删除 SQLite 运行分支、存储实现、迁移脚本与本地数据库文件，避免两套业务语义长期漂移。
- 保留 PostgreSQL 的 schema 迁移、数据引导和导出能力；知识卡、AI4I、MetroPT-3 的导入均走 `server/db/pg-evidence.ts`。
- 与学习领域无关的资料示例中出现的 `database.sqlite` 文本不属于项目 SQLite 依赖，不处理。

## 可选 Web 补全检索决策（2026-08-31）

- SearXNG 作为本机可选适配器接在 PostgreSQL 的 SQL/文档混合检索之后，不新增业务数据库，也不会替代 pgvector、全文检索或结构化查询。
- 自动触发仅限库内文档证据不足、任务明确需要近期/标准/文献资料；Claim 反证复核可以显式强制补查。默认不启用，服务异常、超时或 403 时无中断地退回本地 EvidencePack。
- 网络侧只处理 SearXNG 的标题、摘要、URL、来源引擎名和发布日期，不爬取网页正文、不写入向量库；结果标为 `web_search`、低可信、待复核，不能单独把声明提升为已交叉验证。每次最多三个不同规范化 URL 进入候选账本，摘要版本带 `summaryOnly` 标记，内部策展会保留它而不直接晋升。
- 外发检索词先经过隐私门禁。出现凭据、邮箱、手机号、身份证号时直接阻断并记录脱敏字段类型；SearXNG 虽本机部署，查询仍会发送到其配置的上游搜索源，不能把它描述成“不出网”。

## 0. 下一阶段架构决策（2026-08-29）

- 当前不重做现有工程底座，核心维护目标是“可解释、可复核、可回放、可对照的可信协同方法”。代码架构与验收口径统一维护在 `docs/挑战杯技术开发总规.md`，内部推进记录在 `task_plan.md` 与 `progress.md`。
- 技术主线锁定为“学情图—声明证据图—协同运行链”：学情决定怎么教，证据约束教什么，运行链记录为何这样决策。
- 引入 VACP 可验证智能体协同协议：每个节点输出不可变结构化 artifact，记录输入引用、公开理由、生产者、散列和 attempt；`context_json` 只作为执行缓存，不再作为唯一审计依据。
- 审核、反方质询和证据裁决需要明确不同 actor 与输入权限；即使复用同一底层模型，也必须上下文隔离，规则结论与 Agent 结论取更严格者。
- 个性化与事实保真分开评测：教学难度、脚手架和路径允许随画像变化，字段含义、数据、证据和因果边界不得漂移。
- 前端保留路径、学习、资源三个页面，只新增轻量“验证”页；任何新增展示必须有真实后端数据支撑，不展示隐式思维链。
- 未经用户明确授权，禁止下载依赖、数据、模型、字体或素材，禁止大规模 live 模型评测；优先使用现有依赖、存量数据、确定性测试和离线回放。
- 裁决产出必须带 outcome（released/revised/rejected）进入运行上下文：修订轮由裁决节点直接入队 generate.resource@nextAttempt 并绕过本轮下游（privacy/finalize 标 skipped）；调度器禁止在 revised 分支执行 skipRemaining——否则修订链刚建即被跳过、运行被错误收尾（2026-08-29 修复的 G10 根因）。
- rejected（修订预算用尽）路径同样固化 publication_decision(released=false)、generation_end 快照与 manifest——fail closed 也要有完整审计链。

## 1. 产品定位

**面向工业设备运维初学者与自动化方向学生的“设备数据分析与故障诊断”训练系统**。学习者最终能力目标：用 Python 读取设备传感器数据、完成查询/统计/异常解释，并产出带证据引用的诊断结论。MetroPT-3（空压机时序，152 万行）是中后期真实案例；AI4I 2020（1 万行、字段清晰）是入门教学数据集。

通用多智能体运行时（src/orchestration 等）只是基础设施；比赛主线永远是闭环：
画像 → 任务理解 → 证据检索 → 个性化资源 → Claim 审核/交叉验证 → 学习交互 → 作答证据 → 状态与路径更新 → 下一轮训练。

## 2. 架构级决策（已落地）

- **数据分层**：PostgreSQL 统一存用户/路径/资产/作答/证据/画像、数据集字段字典、`metro_readings` 时序、`dataset_rows` 通用表格行和 `document_chunks`（全文 + pgvector）。
- **审核门禁**：资源发布固定走 `buildEvidencePack → buildResourceDraft → auditResource → resolveResourcePublication`；`corroborated` 才入库，否则不落库。Claim 逐条核对证据绑定与数字一致性；代码块与数据表格不作为事实声明审核。
- **路径节点建议**：`computeNodeRecommendation` 纯函数（补强/保持节奏/可进阶/暂无记录），从 `learner_skill_states` + 讲义掌握反馈确定性推导，理由带真实数字；节点 `user_status` 只能由用户手动设置。
- **知识点贯通**：资源绑定当前路径节点的 `knowledgePointId`（`buildResourceDraft` 第 5 参），作答技能状态因此能对应到节点。
- **账号隔离**：learnerId 一律来自 Cookie 会话；按账号隔离路径/资产/作答/笔记。
- **证据规则**：结构化查询（SQL 抽样代表性数据行）+ PostgreSQL 全文/向量混合检索合并为 EvidencePack；用户上传文件仅作会话级临时参考，不写库、不改画像。
- **服务边界**：学习产品与设置 API 统一使用 Cookie 登录；证据列表按 learnerId 隔离。旧通用 `/api/sessions/*`、通用聊天、任务澄清和 WebSocket 服务已移除，通用多智能体能力只作为内部库保留。
- **MetroPT-3 数据准备**：仓库不提交 208 MB 原始 CSV；`pnpm data:metropt` 从 UCI 官方地址下载并校验，后端发现 CSV 后自动导入 `metro_readings`。未安装完整数据时仍可使用 AI4I、MetroPT-3 字段说明和故障窗口文档。

## 3. 数据层范式（2026-08-28 定）

目标是“换数据不换架构”，垂直领域可整体替换：

1. **知识文档**：往 `data/knowledge/*.md` 放带极简 frontmatter（id/title/source/locator/dataset）的卡片，服务启动自动入库参与 FTS 检索，删掉即失效。已有 14 张卡（Python/pandas/清洗/时序/统计/可视化/AI4I 字段与故障机理/证据边界/阈值法）。
2. **表格数据集**：`server/db/pg-evidence.ts` 的 `importCsvDatasetPg`（声明式来源 + SHA256 校验和，文件没变跳过、换了自动重导入），行进 `dataset_rows`，字段进 `dataset_fields`（labelFields 标故障标签列）。
3. **代表性证据**：`sampleDatasetRows` 按标签抽故障正样本 + 正常行，作为结构化证据进入每个 EvidencePack；讲义“数据摘录”表格按主题关键词选数据集（pandas/AI4I 主题 → AI4I 行；压缩机主题 → MetroPT-3 行）。
4. **新增一个垂直领域 = 一份 CSV + 一组知识卡 + 一条 `importCsvDatasetPg` 配置 + 一份路径生成提示词**，前端和编排完全复用。

## 4. 讲义模板（已落地）

- 简单模板：概念段落 + 要点列表 + **Python 代码示例块**（type=code，pandas 读取/观察/筛选故障样本）+ **代表性数据摘录表**（type=table，主标签列在前，附可回溯定位）+ 证据卡。
- 资源页渲染器已支持 code/table；MD 导出同步支持。分层习题有 L1/L2/L3 三级题 + 判分 + 解析 + 耗时记录。
- 更难的“逐流程模板”（接触数据 → 清洗 → 分析 → 结论的连续实操）留待下一轮，从 practice_guide 升级。

## 5. 差异化测试账号方案（赛题硬要求）

赛题提交要求 **≥2 组**差异化学习者初始学情数据源（含画像输入、协同中间数据、生成资源的完整输入输出示例）；评分“实用价值”30 分想要 **≥3 组**画像测试用例。方案：注册 3 个演示账号，画像显著不同，路径由 LLM 按 onboarding 输入生成（本身就是差异化的第一层证明）：

| 账号 | 画像设定 | 预期差异 |
|---|---|---|
| learner-a | 高职机电专业，Python 零基础，目标“看懂数据” | 路径以 Python 基础/CSV 认知开头的重基础分支 |
| learner-b | 自动化本科，会 MATLAB/基础 Python，目标“做异常检测” | 快速进入时序/统计/异常检测分支 |
| learner-c | 企业在职运维（转岗），每周时间少，目标“会做诊断报告” | 侧重传感器语义、诊断逻辑、报告输出分支 |

每组产出可导出的输入输出包（画像 JSON + 聊天记录 + 生成资产 + 作答记录），作为“初始学情数据源”提交物。**演示与录制前统一执行，冒烟数据不复用。**

## 6. 演示视频叙事（≤10 分钟，2026-08-29 按当前产品更新）

1. **0:00-1:00 差异化输入**：learner-foundation 与 learner-advanced 画像对比；展示建档向导与强制 12 题入学诊断（逐题作答、服务端判分、维度小结）；两棵不同的知识路径树（对比画面）。
2. **1:00-3:30 协同生成**：learner-foundation 在某节点发起讲义协同——中栏群聊逐条冒泡（学情建模 → 双路并行检索 → 领域分析 → 生成），顶部 SSE 实时节点状态条；讲义含 Python 代码块与 AI4I 数据摘录表；讲解混合检索（全文+向量 RRF）与证据定位。
3. **3:30-5:00 幻觉防控与可信辩论**：Claim 逐条审核计数（支持/待复核/无证据）；反方质询议题（规则 + 独立批评 Agent 补充）；裁决 Agent 判决与发布门禁；演示一次"证据不足 → 修订 → 复审"的真实修订环。
4. **5:00-6:30 学习与作答**：资源页翻页阅读、页笔记、掌握反馈；做分层习题（后端判分、解析、耗时）。
5. **6:30-7:30 动态决策与追问**：作答后路径页节点出现"建议补强/可进阶"徽标（带真实作答数字）；画像弹窗展示盲区/难度匹配曲线/资源匹配；节点"启发式追问"——苏格拉底会话两轮，展示 BKT 置信度实时上升，终止后一键生成补强讲义。
6. **7:30-9:00 数据与隐私**：隐私合规门禁（临时参考只审计不保存）；证据包导出（三账号 JSON：画像+诊断+DAG+声明图+裁决+资源）；账号隔离演示（跨账号资产不可见）。
7. **9:00-10:00 收尾**：三账号差异化路径对比 + 一句话架构（PostgreSQL 单数据源 + BullMQ 动态 DAG，换数据集即换领域）。

## 7. 明确不做

- 不做通用聊天机器人叙事；不堆 Agent 数量；不自动改节点状态；不把模型自评当匹配度；原始 CSV 不做向量化；不依赖用户上传资料才能演示。

## 8. 界面反馈收口（2026-08-30）

- 当前运行环境实际使用 PostgreSQL；设置页已改为读取真实数据源、个人记录数量，并提供学习数据导出与审计记录清理。
- 画像更新改为确定性快照：只有诊断、作答、资源反馈或学习时长等新增证据时才更新；无新增证据会返回 `updated=false`。
- 头像支持直接点击头像更换，移除重复提示；协同运行状态与公开事件在完成后保留，路径页协同过程默认展开。
- 资源类型收敛为讲义、习题、PPT、知识脉络，并新增资源问答记忆对话；验证页三张卡片已改为说明真实用途。

## 9. 验证页与头像稳定性（2026-08-30）

- 验证页不再把运行 UUID 当作界面信息，也移除页眉重复标题；前后对照改用轮次、起始/收尾快照和可用性指标呈现。
- 检索后策略改为显示证据覆盖、校验强度和附加约束数量，均来自当前运行记录，不再展示过长的内部说明。
- 头像接口统一返回带诊断完成状态的用户对象；此前更新头像后状态字段缺失，会让页面误回入学诊断，表现为崩溃。
- 图片上传增加类型、大小、解析和尺寸校验，失败只在画像弹窗内提示。

## 10. 学习空间交互收敛（2026-08-31）

- 浏览器原生 `confirm` 会出现宿主名、黑色默认按钮并脱离产品视觉；学习对话与资源问答共用产品内确认面板后，清理范围与保留内容可直接、克制地说明。
- “前置 / 分支 / 应用”并非可在学习页直接静默执行的数据库修改；学习页将当前节点与意图写入短时本地预填，跳转到路径页让用户确认请求，避免把“准备调整”伪装成“已调整”。
- 历史运行恢复会合并服务端轨迹与本地快照，导致相同步骤在进度条重复出现。展示层按节点键保留最新状态，完成数恢复为 10 项且不再触发 React 重复 key 警告。
# 2026-08-31 本轮界面反馈复核

- 浏览器实测：路径页与学习页的路径节点点击当前确实能更新“当前节点”，但当前详情区在右侧面板底部、状态提示弱，容易被误判为功能消失；本轮会强化选中态、标题同步和详情区层级。
- 资源页已有元数据、标签编辑、笔记引用和导出接口；缺口是顶部工具栏过密、导出只有单一 Markdown 入口、章节按钮与 PPT 页码控制缺乏主次、知识脉络的 Mermaid 降级文案过重。
- 项目是桌面端固定三栏工作区，不能用移动端折叠逻辑解决空间问题；应收窄资源文件栏和右侧辅助栏，把阅读正文作为主视觉。
# 2026-08-31 文件清理复核

- 根目录报名表与提交包内归档副本 SHA-256 相同，根目录副本已移除；当前归档位置为 `提交包-湖南师范大学—佘硕—智辩无幻—17267081925/参赛报名表.pdf`。
- `docs/` 只保留当前代码唯一技术基线；README 负责入口说明，提交包负责评委材料，内部记录负责过程与决策。提交文档中的测试基线统一为 26 个测试文件 / 203 个用例。
- 仅清理过期运行日志；`data/`、`.im-training-agent/`、`dist/`、`node_modules/` 和用户提交包均保留。

# 2026-09-02 当前加固发现

- 正式资源类型只有讲义、分层习题、PPT、知识脉络；追问弹窗遗留的 `challenge_task` 没有生成器或入库类型，会导致终态操作不可达。
- 工作台曾在 effect 中同步清空或重置状态，并从 effect 间接启动数据加载；快速切换时可能显示旧内容或引发级联渲染。现在均改为在异步请求回调或明确的用户操作中写入状态。
- 已加载阅读器与当前选中资源按 ID 绑定；在新资源返回前不再借用上一资源内容，避免标题、正文和笔记不一致。

# 2026-09-02 资源生成质量验收

- 真实生成回归确认：四类资源均能从当前节点和本轮证据出发完成生成并落库，不能只依赖固定模板的短结果。
- 讲义和 PPT 即使最终自动审核给出 `revise`，也保留完整的 LLM 成品供学习者查看和复核，不再退回到只有几行的规则短模板；分层习题和知识脉络通过自动审核并发布。
- 生成器兜底内容已扩展为可学习的完整结构；示例代码从证据包动态提取数据集、文件名、时间字段和数值字段，不再固定写死 AI4I 或与当前节点无关的字段。
- 端到端习题作答验证确认提交按钮、判分、解析和重新提交闭环可用。
- PPT 补测确认节点焦点修复有效：学习页、路径画布和任务请求均指向“数据清洗与时间字段”，而不是路径首节点；最终 8 页模型内容通过三轮审核并保存为正式资源。

# 2026-09-03 挑战杯提交包收口

- 赛题要求的材料文档、源码/部署/单元测试、一个垂直知识库切片及不少于两组差异化画像输入—输出示例均已映射到提交包；PPT 与演示视频只保留正式目录，等待最终成品放入，未使用占位文件。
- 作品设计实现方案补齐端到端流程、双图谱、可信治理、协同调用时序、测试口径与赛题覆盖矩阵；附带一份经结构与视觉校验的可交互系统架构图。
- 三组真实运行导出均通过导出校验与重放：讲义、PPT、分层习题、知识脉络四类资源均具备可追溯输入、中间协同数据、证据包与最终输出；60 个离线金标案例和动态规划消融实验均已实际运行。
- 源码压缩包重新按白名单构建为 284 个必要条目，保留代码、测试、部署、锁文件、知识 Markdown 切片、金标和三组导出；剔除缓存、原始资料、生成报告、内部工作记录、环境变量与依赖目录。最终 SHA-256：`84C9692540914B7C76E93F29D1E5ACD790AF6B313C06B84A1AB0765937AF71AC`。
- 发布前复核通过：27 个测试文件 / 204 项用例、后端类型检查和前端生产构建均通过；测试数据根目录新增最小导航说明，包内必需静态文件 12/12 存在。
