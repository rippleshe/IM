# 智辩无幻 技术开发优化文档

> 基于挑战杯赛题评审标准与系统现状的技术层面优化指引。  
> 复核日期：2026-09-02

---

## 一、技术现状基线

### 1.1 已落地的核心技术能力

| 模块 | 实现状态 | 关键文件 |
|:---|:---|:---|
| 10 Actor VACP 协同协议 | ✅ 生产可用 | `server/runs/executor.ts`, `protocol.ts`, `artifacts.ts` |
| BKT 贝叶斯知识追踪 | ✅ 纯函数 + 审计链 | `src/learning/bkt.ts`, `decision.ts` |
| 难度自适应校准 | ✅ 65%-80% 教学区间 | `src/learning/difficulty.ts` |
| Claim 原子化审核与辩论裁决 | ✅ Fail-Closed | `src/learning/audit.ts`, `claim-verification.ts` |
| PG 全文 + pgvector 混合检索 + RRF | ✅ 降级安全 | `server/db/pg-retrieval.ts`, `pg-evidence.ts` |
| 4 类资源生成（讲义/PPT/习题/脉络） | ✅ 可交付 | `src/learning/resource-builder.ts`, `server/prompts.ts` |
| 苏格拉底启发式追问 | ✅ 2-5 轮收敛 | `server/guidance-service.ts` |
| 知识库来源治理与策展门禁 | ✅ 候选→正式 | `server/knowledge-governance.ts`, `knowledge-curator.ts` |
| SSE 实时事件流 + 断点续传 | ✅ seq 单调递增 | `server/runs/routes.ts`, `events.ts` |
| 191 项单测 / 0 TypeScript 错误 | ✅ 通过 | 26 个测试文件 |

### 1.2 按赛题评审维度的技术差距

**作品完整性（30 分）**  
- 缺口：赛题要求"至少 3 种形态"，当前 4 种已达标；但"实操指南"与"挑战任务"仅以任务文案体现，不是可独立交互和导出的资产类型。扩展到 6 种可拉开差距。

**技术创新性（25 分）**  
- 缺口：Claim 辩论裁决机制有突破性，但前端只以文字消息展示协同过程，DAG 编排和声明演进缺乏直观可视化；全文检索中文分词精度受限于 PG `simple` 分词器。

**用户体验（15 分）**  
- 缺口：协同调度过程没有图形化 DAG 进度组件；学情报告缺乏可导出的结构化文档。

**实用价值（30 分）**  
- 缺口：知识库 19 份候选资料未批量晋升正式切片；嵌入向量运维缺少可观测状态；模型调用缺乏取消机制和限流保护，演示时若遇 429 或超时会中断流程。

---

## 二、资源资产类型扩展（讲义/PPT/习题/脉络 → +实操指南 +挑战任务）

### 2.1 目标

将可独立交付的学习资源从 4 种扩展到 6 种，每种都具备：生成提示词 → LLM 结构化输出 → 质量校验 → Claim 审核 → 前端渲染 → 导出。

### 2.2 类型定义

修改 `src/learning/types.ts`：

```typescript
export type LearningResourceType =
  | 'lecture'
  | 'presentation'
  | 'tiered_quiz'
  | 'concept_map'
  | 'practice_guide'    // 新增：步骤式实操手册
  | 'challenge_task';   // 新增：综合实战挑战
```

### 2.3 提示词契约

在 `server/prompts.ts` 的 `RESOURCE_TYPE_LABELS` 和 `TYPE_GUIDES` 中追加：

```typescript
// RESOURCE_TYPE_LABELS
practice_guide: '实操指南',
challenge_task: '挑战任务',
```

**practice_guide 写作大纲**

```
这是「实操指南」：一份可按步骤逐项执行的动手操作手册，学习者可以边读边做。
写作标准：
- 5 到 7 个 steps，每步一个明确的、可验证的操作目标，按"准备环境 → 获取数据 → 清洗
  检查 → 核心操作 → 结果验证 → 整理归档"的工程流程组织；
- 每步必须包含 instruction（50-120 字的操作指令）、expected（预期可验证的结果描述）、
  pitfall（该步最常见的 1-2 个错误及原因）；
- 至少 3 个步骤提供可直接运行的 Python/pandas 代码（code 字段），代码对给定数据集可
  执行、10-25 行、逐行中文注释；
- 最后给 checkpoint（3-4 条自检清单，每条都有明确的通过标准）和 summary（60-100 字
  的操作要点回顾）。
```

**practice_guide JSON schema**

```
{"title","lead","objectives",
 "steps":[{"heading","instruction","code":{...}可选,"expected","pitfall":"常见错误"}],
 "checkpoint":["自检项"],"summary":"操作要点回顾"}
```

**challenge_task 写作大纲**

```
这是「挑战任务」：一份综合性实战挑战，学习者独立完成一个完整的分析或诊断任务并提交成果。
写作标准：
- scenario（150-300 字的工业场景描述，含具体设备/产线背景、给定数据范围和需要解决的
  工程问题，来自给定证据）；
- objectives（3-5 条挑战目标，按"发现 → 分析 → 判断 → 行动建议"递进，每条以动词
  开头）；
- constraints（2-3 条约束条件：可用数据范围、工具限制、时间建议）；
- deliverables（2-3 项需要交付的成果物，如"异常诊断报告""可复现分析脚本"）；
- rubric（4-5 条评分维度，每条描述"合格"与"优秀"的边界）；
- hints（3 条分层提示，从方向性→方法性→具体操作递进，学习者可按需展开）。
```

**challenge_task JSON schema**

```
{"title","lead","objectives",
 "scenario":"工业场景",
 "constraints":["约束"],
 "deliverables":[{"name","description"}],
 "rubric":[{"dimension","passing","excellent"}],
 "hints":[{"level":"direction|method|detail","content"}]}
```

### 2.4 资源构建器扩展

修改 `src/learning/resource-builder.ts`：

1. `buildResourceDraft()` — 为两种新类型构建初始草稿骨架（opening block + task block + evidence blocks）
2. `parseLlmResourceDraft()` — 增加两种类型的 JSON 解析与 ResourceBlock 组装：
   - `practice_guide`：每个 step 映射为 heading + paragraph(instruction) + code(可选) + list(expected + pitfall)
   - `challenge_task`：scenario 映射为 paragraph，objectives/constraints/deliverables 各映射为 list，rubric 映射为 table，hints 映射为折叠面板
3. `validateLlmResourceDraftQuality()` — 增加两种类型的质量校验：
   - `practice_guide`：steps 数量 ≥5、每步有 instruction 和 expected、至少 3 步有 code
   - `challenge_task`：scenario ≥ 100 字、objectives ≥3 条、rubric ≥3 条、hints ≥2 条

`schemaHint()` 函数追加两种类型的 JSON schema 描述字符串。

### 2.5 执行器适配

修改 `server/runs/executor.ts` 中的 `executeGenerateResource()`：

- `resourceGenerationSystem(context)` 已经通过 `TYPE_GUIDES[type]` 自动路由到对应提示词，无需改主逻辑
- Claim 审核逻辑（`executeAuditClaims`）对 ResourceBlock 遍历提取文本，新类型的 block 结构天然兼容
- 难度校准中 `practice_guide` 的 scaffold 为 `'medium'`，`challenge_task` 为 `'low'`

### 2.6 前端渲染

修改 `web/src/components/resource-workbench.tsx`：

**practice_guide 渲染方案**  
步骤卡片式布局，每步：序号圆标 + 标题 → 操作指令段落 → 代码块（如有） → 预期结果（绿底条） → 常见错误（黄底折叠面板）。底部 checkpoint 为 checklist 交互组件。

**challenge_task 渲染方案**  
场景描述为蓝底引用块 → 挑战目标编号列表 → 约束条件为灰底卡片 → 交付物为表格 → 评分标准为双列对比表（合格 / 优秀） → 分层提示为渐进展开（首条可见、其余折叠，点击后标注"已使用提示"）。

### 2.7 导出适配

`server/index.ts` 中 `resourceToMarkdown()` 和 `resourceToPresentation()` 已对 ResourceBlock 类型做 switch 分发，新 block 类型会被 `paragraph`/`list`/`table`/`code` 等既有分支覆盖。

---

## 三、多智能体协同过程可视化

### 3.1 实时 DAG 进度组件

**目标**：在学习工作台的协同面板顶部增加一个轻量级的 DAG 进度条，实时反映 10 个节点的执行状态。

**组件设计**（新建 `web/src/components/dag-progress.tsx`）

```
[学情] ─┐
[数据] ─┼→ [领域] → [生成] → [审核] → [质询] → [裁决] → [隐私] → [发布]
[文档] ─┘                      ↑_____修订环________|
```

状态枚举与颜色：

| 状态 | 视觉 |
|:---|:---|
| `pending` | 灰色空心圆 |
| `running` | 蓝色脉冲动画圆 |
| `succeeded` | 绿色实心圆 + ✓ |
| `failed` | 红色实心圆 + ✗ |
| `revision` | 橙色虚线回连箭头 + "第N轮" |

实现要点：
- 固定 10 节点布局，用 CSS Grid 或 flexbox（不用 SVG，保持轻量）
- 数据源：监听 SSE 的 `node.started` / `node.succeeded` / `node.failed` / `run.revision` 事件，在 `learning-workbench.tsx` 中已有 EventSource 订阅，只需将 nodeKey→status 映射传入子组件
- 点击节点弹出该节点的公开摘要（从 `run_events` 读取）
- 修订环：当收到 `run.revision` 事件时，在 `generate.resource` 与 `adjudicate.verdict` 之间渲染虚线回连

### 3.2 Claim 声明演进图谱

**目标**：在验证工作台 `validation-workbench.tsx` 的运行详情中增加 Claim 演进追溯视图。

**数据源**：  
- `GET /api/learning/runs/:runId/trace` 已返回 `claims` 数组，含 `logical_key`、`supersedes_claim_id`、`verdict`、`round`
- 按 `logical_key` 分组，同一逻辑键的多轮 claim 构成演进链

**渲染方案**：  
- 每条逻辑键一行：初稿声明 → 箭头 → 终稿声明
- 颜色编码：supported=绿、review=黄、unsupported=红
- 可展开查看绑定的证据 ID 和核验 checks
- 顶部统计：总事实声明数、初稿通过率、终稿通过率、修订轮次

### 3.3 学情报告可视化导出

**目标**：在画像弹窗 `profile-dialog.tsx` 底部增加"生成学情报告"按钮，一键生成可打印的 HTML 报告。

**报告内容结构**：

```
1. 学习者画像摘要（匿名化）
   - 入学背景、目标、诊断结果
   - 五维能力雷达（复用 ProfileRadar SVG）

2. 知识盲区定位
   - BKT 掌握热力图（节点颜色深浅 = pMastery）
   - 薄弱环节列表（pMastery < 0.45 的知识点 + 建议动作）

3. 资源匹配度报告
   - 历次难度校准记录表（targetDifficulty, expectedSuccessRate）
   - 预计成功率 vs 实际表现趋势折线

4. 多智能体协同审计
   - 总运行数、通过率、平均修订轮次
   - 幻觉治理效果：初稿→终稿声明改善统计
```

**实现路径**：
- 后端新增 `GET /api/learning/report`，聚合 BKT 状态、路径图、学习决策、运行记录到一个 JSON
- 前端新建 `web/src/components/report-view.tsx`，接收 JSON 渲染为包含 SVG 图表的 HTML
- `window.print()` 导出为 PDF，或 `Blob` 下载 HTML

---

## 四、知识库检索质量升级

### 4.1 候选资料批量策展

当前 19 份受管资料全部处于 `candidate` 状态。需要执行策展门禁使合格资料晋升为正式知识库切片。

```bash
pnpm knowledge:curate
```

策展门禁（`server/knowledge-curator.ts`）的三项阈值：
- `relevance ≥ 0.68` — 与工业设备数据分析的相关性
- `quality ≥ 0.65` — 文本完整性和结构化程度
- `duplicateRisk ≤ 0.25` — 与既有切片的重复风险

通过后事务性写入版本化切片，失败保持候选不污染正式库。

### 4.2 嵌入向量运维闭环

**问题**：新晋升的知识切片如果没有 embedding 向量，就不会参与向量检索路径（只能走全文检索），混合检索精度受损。

**方案**：在 `document_chunks` 的 metadata 中记录嵌入状态：

```typescript
type EmbeddingStatus = 'pending' | 'in_progress' | 'embedded' | 'failed';
```

修改 `server/db/pg-evidence.ts` 中的切片创建逻辑：
- 新切片入库时默认 `embedding_status = 'pending'`
- 嵌入回填脚本 `scripts/backfill-embeddings.ts` 批量处理 pending 状态
- 提供 `pnpm knowledge:embed-status` 查看进度
- 失败重试：最多 3 次，间隔指数退避（2s/4s/8s）
- Reranker 预留：RRF 后增加 BGE-reranker 交叉重排的接口和配置开关（默认关闭，超时降级到 RRF）

### 4.3 Node 端中文分词入库

**问题**：PostgreSQL `'simple'` 分词器不做中文分词，长专业术语（如"预测性维护故障诊断"）无法被拆分，全文检索召回率受限。

**方案**：在切片入库时用 Node 端分词库预处理 `search_text`。

修改 `server/db/pg-evidence.ts`：

```typescript
import Segment from 'segment';

const segment = new Segment();
segment.useDefault(); // 加载默认词典

function buildSearchText(content: string, title?: string): string {
  // 对内容做分词，生成空格连接的词序列
  const words = segment.doSegment(content, { simple: true });
  const wordStr = words.map(w => w.w).join(' ');
  // search_text = 原文 + 分词结果（PG FTS 同时匹配长句和短词）
  const prefix = title ? `${title} ` : '';
  return `${prefix}${content}\n${wordStr}`;
}
```

选择 `segment` 包（纯 JS，无 native 依赖，约 4MB 词典）而非 `nodejieba`（需要编译 C++ 扩展），降低部署门槛。

替代方案：如果部署环境支持，可使用 PostgreSQL 的 `pg_jieba` 或 `zhparser` 扩展做服务端分词，效果更好但需要数据库扩展安装权限。

### 4.4 检索质量验收

- 在黄金知识点评测集上验证：新资料入库后 `Recall@20` 不低于当前基线
- 中文分词启用前后对比：对 10 个专业术语查询的 FTS 命中率
- 嵌入回填完成后对比：RRF 融合有向量 vs 无向量的 `nDCG@8`

---

## 五、模型调用健壮性工程

### 5.1 AbortSignal 超时取消

**问题**：当前 `withTimeout()` 只是 `Promise.race` + reject，底层 HTTP 连接仍在消耗模型 API Token 和服务端资源。

**方案**：修改 `server/study-runtime.ts` 的 `MultiModelClient`，在所有模型调用入口传入 `AbortController.signal`：

```typescript
// study-runtime.ts
export async function withAbortableTimeout<T>(
  fn: (signal: AbortSignal) => Promise<T>,
  timeoutMs: number,
  label: string,
): Promise<T> {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), timeoutMs);
  try {
    return await fn(controller.signal);
  } finally {
    clearTimeout(timer);
  }
}

// MultiModelClient.chat() / .streamText() / .simple()
// 将 signal 透传到 OpenAI SDK 的 fetch 请求
```

在 `server/runs/executor.ts` 的各节点执行函数中替换现有的 `withTimeout` 调用。

### 5.2 令牌桶请求限流

**问题**：多用户并发触发 heavy 生成时可能遇到服务商 HTTP 429。

**方案**：新建 `server/rate-limiter.ts`：

```typescript
export class TokenBucketLimiter {
  private tokens: number;
  private lastRefill: number;

  constructor(
    private readonly maxTokens: number = 10,
    private readonly refillRate: number = 10 / 60, // 10 RPM
  ) {
    this.tokens = maxTokens;
    this.lastRefill = Date.now();
  }

  async acquire(): Promise<void> {
    this.refill();
    while (this.tokens < 1) {
      await new Promise(r => setTimeout(r, 200));
      this.refill();
    }
    this.tokens -= 1;
  }

  private refill(): void {
    const now = Date.now();
    const elapsed = (now - this.lastRefill) / 1000;
    this.tokens = Math.min(this.maxTokens, this.tokens + elapsed * this.refillRate);
    this.lastRefill = now;
  }
}
```

集成到 `MultiModelClient` 的 `chat()`、`streamText()`、`simple()` 入口，在发送请求前 `await limiter.acquire()`。

RPM 上限通过 `.env` 配置：`MODEL_RPM_LIMIT=10`。

### 5.3 HTTP 429 智能退避重试

在 `MultiModelClient` 内部包装 fetch 层：

```typescript
async function fetchWithRetry(
  request: () => Promise<Response>,
  maxRetries = 3,
): Promise<Response> {
  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    const response = await request();
    if (response.status !== 429 || attempt === maxRetries) return response;
    const retryAfter = parseInt(response.headers.get('Retry-After') || '', 10);
    const delay = (retryAfter > 0 ? retryAfter : Math.pow(2, attempt + 1)) * 1000;
    console.warn(`[model] 429 限流，${delay / 1000}s 后重试 (${attempt + 1}/${maxRetries})`);
    await new Promise(r => setTimeout(r, delay));
  }
  throw new Error('模型服务持续限流');
}
```

### 5.4 运行取消信号贯穿

**问题**：用户点击"取消运行"后，已入队的 BullMQ 任务虽然被移除，但正在执行的节点（特别是 heavy 生成节点）仍会继续调用模型。

**方案**：  
- 在 `study_runs` 表增加 `cancelled_at` 字段
- `executor.ts` 中每个节点执行函数的循环入口检查 `run.status === 'cancelled'`
- 将 `AbortController` 与运行取消信号绑定：取消运行时 `controller.abort()` 立即中断正在进行的模型请求

---

## 六、BKT 与决策算法增强

### 6.1 多维度 BKT 状态追踪

**现状**：当前 BKT 按 `knowledgePointId` 单维度追踪掌握概率。赛题要求"知识盲区定位"和"学习路径规划图"，可以通过扩展 BKT 的维度来增强。

**方案**：在 `learner_skill_states` 中追加维度级聚合视图，不改底层 BKT 纯函数：

```typescript
// 新增 src/learning/dimension-aggregation.ts
export type SkillDimension =
  | 'python_programming'
  | 'data_processing'
  | 'statistics'
  | 'timeseries_analysis'
  | 'equipment_diagnosis';

// 12 题诊断中已有 dimension 字段，对应 5 大维度
// 通过知识点到维度的映射表聚合各维度的平均 pMastery 和 confidence
export function aggregateDimensionMastery(
  skillStates: BktState[],
  knowledgePointDimensions: Map<string, SkillDimension>,
): Map<SkillDimension, { avgMastery: number; avgConfidence: number; count: number }>
```

前端 `ProfileRadar` 五维雷达图的数据源从静态配置改为此函数的实时计算结果。

### 6.2 决策引擎与路径推荐联动

**现状**：`decideLearningNextStep()` 输出 `remediate | continue | advance | collect_more_evidence` + `recommendedResourceType`，但决策结论仅持久化到 `learning_decisions` 表，没有自动驱动路径节点状态更新和"推荐下一步"的 UI 提示。

**方案**：

1. 在 `server/decision-service.ts` 中，`recordLearningDecision()` 写入决策后，根据决策类型更新路径节点状态：
   - `advance` → 将当前知识点节点标记为 `completed`（如果 pMastery ≥ 0.72 且 confidence ≥ 0.6）
   - `remediate` → 找到先修缺口最大的节点，在路径页高亮为"建议补强"

2. 在路径工作台 `learning-path-workbench.tsx` 中，节点渲染时读取最新决策：
   - `remediate` 节点显示橙色标记 + "建议补强"
   - `advance` 节点的后继节点显示绿色标记 + "可进阶"
   - `collect_more_evidence` 节点显示蓝色标记 + "建议继续学习"

### 6.3 诊断题库扩展与自适应

**现状**：12 题固定诊断题覆盖 5 个维度 × 3 个难度层级（L1/L2/L3），每维度仅 2-3 题。

**方案**（中优先级，可选实施）：

1. 将诊断题库从硬编码扩展到 20-24 题
2. 支持自适应诊断：根据前 6 题的作答模式动态选择后续题目（计算机自适应测试 CAT 思路）
3. 实现：在 `src/learning/diagnostic.ts` 中增加 `adaptiveDiagnosticSelector()`，按信息增益最大化原则从题库中选择下一题

---

## 七、协同协议与审计能力增强

### 7.1 反证检索闭环

**现状**：反方批评智能体（`debate.challenge`）可以发出 `counterevidence_request` 类型的质询请求（`argument` 字段为检索词），但当前只记录了请求本身，没有自动执行反证检索并回填结果。

**方案**：  
修改 `server/runs/executor.ts` 中的 `executeDebateChallenge()`：

```typescript
// 当反方 issues 中存在 counterevidence_request 时
for (const issue of issues.filter(i => i.issueType === 'counterevidence_request')) {
  // 1. 以 issue.argument 为查询词执行知识库混合检索
  const counterPack = await evidenceService.buildEvidencePack(issue.argument, { learnerId });
  // 2. 将检索结果写入 RunContext.counterevidence
  // 3. 如果反证检索命中了与初稿声明矛盾的内容，将 issue.status 升级为 conflict
  // 4. 反证结果一并传给裁决节点作为裁判输入
}
```

如果部署了 SearXNG，反证检索可同时查询本地知识库和网络源。

### 7.2 产物完整性自动校验

**现状**：`pnpm replay:run` 可以离线复算和校验，但需要手动执行。

**方案**：
- 在 `finalize.publish` 节点成功后自动执行一次轻量级完整性校验（`verifyRunIntegrity()`），结果写入 `run_events`
- 校验项：产物散列一致性、DAG 闭合性、Claim-Evidence 边完整性、裁决只紧不松、发布 fail-closed
- 校验结果在验证工作台自动展示，无需用户手动触发

### 7.3 跨运行声明溯源

**现状**：Claim 溯源仅在单次运行内追踪（通过 `logical_key` 和 `supersedes_claim_id`）。

**方案**（中优先级）：  
- 在 `claims` 表增加 `related_run_ids` 字段
- 当同一学习者对同一知识点发起新运行时，检索历史运行中的相关 Claim，为审核节点提供"历史声明参考"
- 前端验证工作台可展示"同知识点历史审核趋势"

---

## 八、前端交互与工程质量

### 8.1 SSE 连接优化

**问题**：每个 SSE 连接（`/api/learning/runs/:runId/events`）独立创建 Redis 订阅客户端，高并发时 Redis 连接数膨胀。

**方案**：在 `server/runs/events.ts` 中实现 RunEventHub 单连接广播：

```typescript
import { EventEmitter } from 'node:events';

class RunEventHub {
  private subscriber: Redis;
  private emitter = new EventEmitter();
  private subscribedChannels = new Set<string>();

  constructor(redisUrl: string) {
    this.subscriber = new Redis(redisUrl);
    this.subscriber.on('message', (channel, message) => {
      this.emitter.emit(channel, JSON.parse(message));
    });
  }

  subscribe(runId: string, handler: (event: RunEvent) => void): () => void {
    const channel = `run:${runId}`;
    this.emitter.on(channel, handler);
    if (!this.subscribedChannels.has(channel)) {
      this.subscriber.subscribe(channel);
      this.subscribedChannels.add(channel);
    }
    // 返回 unsubscribe 函数
    return () => {
      this.emitter.off(channel, handler);
      if (this.emitter.listenerCount(channel) === 0) {
        this.subscriber.unsubscribe(channel);
        this.subscribedChannels.delete(channel);
      }
    };
  }
}

export const runEventHub = new RunEventHub(process.env.REDIS_URL || 'redis://localhost:6379');
```

在 `server/runs/routes.ts` 的 SSE 路由中替换现有的 `createRunSubscriber()`。

### 8.2 前端 API 客户端统一

**问题**：各组件散落着重复的 `fetch(apiBase + '/...'`, `{ credentials: 'include' })` 调用，URL 拼接和错误处理逻辑分散。

**方案**：新建 `web/src/lib/api.ts`：

```typescript
const API_BASE = process.env.NEXT_PUBLIC_API_BASE || 'http://localhost:3001';

class ApiClient {
  private async request<T>(path: string, options?: RequestInit): Promise<T> {
    const response = await fetch(`${API_BASE}${path}`, {
      ...options,
      credentials: 'include',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
    });
    if (response.status === 401) {
      window.dispatchEvent(new CustomEvent('im-training-agent:session-expired'));
      throw new Error('请先登录');
    }
    const data = await response.json();
    if (!data.success) throw new Error(data.error || '请求失败');
    return data;
  }

  get<T>(path: string) { return this.request<T>(path); }
  post<T>(path: string, body?: unknown) {
    return this.request<T>(path, { method: 'POST', body: body ? JSON.stringify(body) : undefined });
  }
  patch<T>(path: string, body?: unknown) {
    return this.request<T>(path, { method: 'PATCH', body: body ? JSON.stringify(body) : undefined });
  }
  delete<T>(path: string) { return this.request<T>(path, { method: 'DELETE' }); }

  /** SSE 连接（返回 EventSource 包装） */
  sse(path: string, handlers: Record<string, (data: unknown) => void>): () => void {
    const source = new EventSource(`${API_BASE}${path}`, { withCredentials: true });
    for (const [event, handler] of Object.entries(handlers)) {
      source.addEventListener(event, (e) => handler(JSON.parse((e as MessageEvent).data)));
    }
    return () => source.close();
  }
}

export const api = new ApiClient();
```

逐步替换各组件中的裸 fetch 调用（不要一次性全改，按组件逐个替换并验证）。

### 8.3 Mermaid 渲染单例化

**问题**：`rich-text.tsx` 中 `import("mermaid")` 每次动态导入后在 `finally` 清除临时 DOM 节点，多个知识脉络图并发渲染时可能出现配置覆盖。

**方案**：封装 Mermaid 渲染单例：

```typescript
// web/src/lib/mermaid-renderer.ts
let mermaidInstance: typeof import('mermaid') | null = null;
let renderQueue: Promise<void> = Promise.resolve();

export async function renderMermaid(
  id: string,
  definition: string,
  container: HTMLElement,
): Promise<void> {
  // 串行化渲染，避免并发配置覆盖
  renderQueue = renderQueue.then(async () => {
    if (!mermaidInstance) {
      const m = await import('mermaid');
      m.default.initialize({ startOnLoad: false, theme: 'neutral', securityLevel: 'loose' });
      mermaidInstance = m;
    }
    try {
      await mermaidInstance.default.parse(definition);
      const { svg } = await mermaidInstance.default.render(id, definition);
      container.innerHTML = svg;
    } catch {
      container.textContent = definition; // 降级为纯文本
    }
  });
  return renderQueue;
}
```

---

## 九、数据合规与隐私增强

### 9.1 临时参考资料的隐私清理时机

**现状**：`redactRunTemporaryReference()` 在运行成功/失败/取消后清理上传正文。但如果 Worker 进程异常退出，正文可能残留。

**方案**：
- 在 `worker.ts` 启动时扫描所有 `status = 'running'` 且 `updated_at` 超过 30 分钟的运行，标记为 `failed` 并执行正文清理
- 增加定时任务（BullMQ repeatable job，每小时一次）扫描孤儿运行

### 9.2 敏感信息检测增强

**现状**：`knowledge-curator.ts` 中的隐私阻断检测手机号、邮箱、身份证号。

**方案**：追加检测模式：

```typescript
const SENSITIVE_PATTERNS = [
  /1[3-9]\d{9}/,               // 手机号
  /\d{15}(\d{2}[\dX])?/,       // 身份证
  /[\w.-]+@[\w.-]+\.\w{2,}/,   // 邮箱
  /(?:密码|password|token|secret|api[_-]?key)\s*[:=]\s*\S+/i,  // 新增：密钥泄露
  /\d{16,19}/,                 // 新增：银行卡号
];
```

---

## 十、跨领域迁移性架构优化

### 10.1 领域配置解耦

**目标**：将当前硬编码在代码中的领域特定内容（工业设备数据诊断）抽取为可配置项，展示系统在不同垂直领域的迁移能力。

**方案**：新建 `domain.config.ts`：

```typescript
export interface DomainConfig {
  /** 领域名称 */
  name: string;
  /** 领域描述（用于路径规划提示词） */
  description: string;
  /** 诊断维度 */
  diagnosticDimensions: string[];
  /** 默认路径模板关键词 */
  pathTemplateKeywords: string[];
  /** 数据集 ID 与匹配正则映射 */
  datasetHints: Array<[string, RegExp]>;
  /** Claim 审核中的领域特定因果关键词 */
  causalHints: string[];
}

export const CURRENT_DOMAIN: DomainConfig = {
  name: '工业设备数据预测与诊断',
  description: '面向工业设备运维初学者的设备数据分析与故障诊断技能训练',
  diagnosticDimensions: ['python_programming', 'data_processing', 'statistics', 'timeseries_analysis', 'equipment_diagnosis'],
  pathTemplateKeywords: ['Python', 'CSV', 'DataFrame', '时间序列', '异常检测', '诊断逻辑'],
  datasetHints: [
    ['ai4i-2020', /pandas|python|csv|预测性维护|machine\s*failure/i],
    ['metropt-3', /压缩机|compressor|泄漏|压力|油温|时序|传感器/i],
  ],
  causalHints: ['因为', '导致', '引发', '造成', '会使得', '故障原因', '根因'],
};
```

然后在 `server/prompts.ts`、`src/learning/diagnostic.ts`、`src/learning/audit.ts`、`server/initial-path.ts` 等处引用 `CURRENT_DOMAIN` 而非硬编码字符串。

迁移到新领域时只需修改 `domain.config.ts` + 导入对应领域的知识库和数据集。

---

## 十一、测试与评测增强

### 11.1 新增资源类型的单元测试

```typescript
// src/learning/resource-builder.test.ts 追加
describe('practice_guide resource', () => {
  it('should build draft with steps structure');
  it('should parse LLM output into ResourceBlocks');
  it('should validate step count >= 5');
  it('should validate code blocks >= 3');
  it('should extract claims from instruction text');
});

describe('challenge_task resource', () => {
  it('should build draft with scenario + rubric structure');
  it('should parse LLM output into ResourceBlocks');
  it('should validate scenario length >= 100');
  it('should validate rubric dimensions >= 3');
});
```

### 11.2 评测案例集扩展

在 `data/evaluation/gold-cases.json` 中追加实操指南和挑战任务的案例：

- 每画像各增加 2 个 `practice_guide` 案例、2 个 `challenge_task` 案例
- 扩展后总案例数：60 + 12 = 72 案例（超过赛题要求的 50 组门槛）
- 评测指标口径不变，新增类型纳入幻觉率和覆盖率分母

### 11.3 消融实验补充

在 `pnpm ablation` 中增加两组消融：

| 消融组 | 对照 | 预期结论 |
|:---|:---|:---|
| A4 中文分词 vs 原始 simple | FTS 命中率 | 分词后专业术语召回率提升 |
| A5 6类资源 vs 4类资源 | 知识覆盖率 | 资源多样性提升覆盖完整度 |

---

## 十二、部署与运维改进

### 12.1 Docker Compose 健康检查

修改 `docker-compose.yml`，为关键服务增加 healthcheck：

```yaml
services:
  api:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/api/auth/me"]
      interval: 30s
      timeout: 5s
      retries: 3
  
  postgres:
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 3s
  
  redis:
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
```

### 12.2 启动自检增强

在 `server/index.ts` 的 `startServer()` 中增加启动前环境自检：

```typescript
async function preflightCheck(): Promise<void> {
  // 1. PostgreSQL 连接
  // 2. Redis 连接
  // 3. 模型 API Key 配置（警告，不阻塞）
  // 4. 嵌入向量 API Key 配置（警告，不阻塞）
  // 5. MetroPT-3 数据可用性
  // 6. 知识库切片数量
  console.log('✅ 环境自检通过');
}
```

---

## 附：优化项速查索引

| # | 优化项 | 涉及文件 | 复杂度 |
|:---:|:---|:---|:---:|
| 1 | 扩展 practice_guide 资源类型 | types.ts, prompts.ts, resource-builder.ts, executor.ts, resource-workbench.tsx | 中 |
| 2 | 扩展 challenge_task 资源类型 | 同上 | 中 |
| 3 | DAG 进度可视化组件 | 新建 dag-progress.tsx, 修改 learning-workbench.tsx | 中 |
| 4 | Claim 声明演进图谱 | 修改 validation-workbench.tsx | 中 |
| 5 | 学情报告导出 | 新建 report-generator.ts, report-view.tsx | 中 |
| 6 | 候选资料批量策展 | knowledge-curator.ts（运维执行） | 低 |
| 7 | 嵌入向量运维闭环 | pg-evidence.ts, scripts/backfill-embeddings.ts | 中 |
| 8 | Node 端中文分词 | pg-evidence.ts, package.json | 低 |
| 9 | AbortSignal 超时取消 | study-runtime.ts, executor.ts | 低 |
| 10 | 令牌桶限流 | 新建 rate-limiter.ts, study-runtime.ts | 低 |
| 11 | HTTP 429 退避重试 | study-runtime.ts | 低 |
| 12 | 运行取消信号贯穿 | executor.ts, service.ts | 中 |
| 13 | BKT 维度聚合 | 新建 dimension-aggregation.ts, profile-dialog.tsx | 低 |
| 14 | 决策-路径联动 | decision-service.ts, learning-path-workbench.tsx | 中 |
| 15 | 反证检索闭环 | executor.ts | 中 |
| 16 | 产物自动完整性校验 | executor.ts, metrics.ts | 低 |
| 17 | Redis SSE 单连接广播 | events.ts, routes.ts | 中 |
| 18 | 前端 API 客户端统一 | 新建 web/src/lib/api.ts | 中 |
| 19 | Mermaid 渲染单例 | 新建 web/src/lib/mermaid-renderer.ts | 低 |
| 20 | 领域配置解耦 | 新建 domain.config.ts, prompts.ts, audit.ts 等 | 中 |
| 21 | 敏感信息检测增强 | knowledge-curator.ts | 低 |
| 22 | Docker 健康检查 | docker-compose.yml | 低 |
| 23 | 启动自检增强 | server/index.ts | 低 |
| 24 | 评测案例扩展到 72 组 | data/evaluation/ | 低 |
