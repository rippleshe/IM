# CLAUDE.md

本文件是 IM-Training-Agent 的强制开发规范。任何 AI、Agent 或开发者在修改本项目之前都必须先阅读并遵守。

## 一、产品边界

IM-Training-Agent 是面向挑战杯“领域知识个性化生成与多智能体协同决策系统研究”的开发项目。底层通用多智能体框架只是基础设施，最终产品主线必须服务于“学情诊断—多智能体协同—个性化资源—校验审核—学习反馈—动态调整”。

## 二、前端保护原则

1. 当前多智能体协同前端已经形成较完整的产品交互，**不得因为个人审美或顺手重构而大改结构、布局、交互链路或协同展示方式**。
2. 修改前端前必须先确认真实问题和业务收益；没有明确问题时，优先保持现状。
3. 需要修改时应做局部、可验证的改进，不能把已经可用的协同体验重新做成通用 Demo。
4. 测试通过只是基本要求，不能把“构建成功、测试全过”当作产品质量本身。

## 三、旧包袱清理原则

1. 原 Pi Multi-Agent 的品牌、截图、发布脚本、社区宣传、无业务依赖的通用开源模板等旧包袱应主动删除或收口。
2. 删除前先确认没有真实代码引用、运行依赖或比赛展示价值；不得为了“清理”破坏当前功能。
3. 旧兼容字段如果仍用于迁移，可以暂时保留，但必须与新品牌隔离，不能重新出现在用户界面和项目主叙事中。
4. 新增文件前先判断是否能修改现有文件解决，避免项目继续膨胀成文档和脚本垃圾场。

## 四、模型与密钥

1. 基础回退模型为通义千问 DashScope OpenAI-compatible API；工作台实际默认模型以 Git 忽略的运行时配置和设置页为准，不得在业务代码中绑定某个本机服务商或模型。
2. API Key 只能放在 `.env`、本机环境变量或其他被 Git 忽略的安全位置。
3. **严禁把真实 API Key、Token、Cookie、密码提交到 Git、README、示例代码或日志中。**
4. `.env.example` 只能放占位符，不得出现真实密钥。
5. 模型目录与能力优先从服务商兼容接口同步；上下文和输出限制属于服务端内部能力，不作为普通用户配置项。接口未返回能力时使用已有缓存和安全预算。

## 五、Git 工作流

1. 开始开发前先查看 `git status`，避免覆盖未知改动。
2. 每轮开发结束必须检查 diff、运行与改动相关的类型检查/构建/测试。
3. 检查通过后，原则上应提交并推送到当前绑定的 GitHub 远端；除非用户明确说本轮不要推送。
4. Commit message 要简短说明实际改动，不写“update”等无意义描述。
5. 推送前再次确认没有密钥、缓存、数据库、构建产物等敏感或无价值文件进入暂存区。

## 六、测试投入原则

1. 测试只投入少量、必要的精力，用于确认核心功能没有被明显破坏。
2. 优先做与本轮改动直接相关的类型检查、关键链路冒烟验证和必要构建；没有明确收益时，不扩写大批测试用例。
3. 禁止为了追求“全绿”、覆盖率数字或形式上的完整 QA，长期停留在补无关测试、修边缘测试或反复跑全量验证上。
4. 如果测试暴露真实业务问题要修；如果只是低价值测试维护，不应挤占核心开发时间。
5. 项目时间优先投入真实功能、用户体验、多智能体协同能力和挑战杯核心业务闭环。

## 七、开发输出规范

开发完成后的用户回复保持简短：说明“改了什么、验证结果、是否已推送”即可，不输出长篇项目总结，除非用户明确要求详细说明。

## 八、本地服务约定

1. 保持前后端开发服务常开供用户随时查看：后端 `npm run server`（3001），前端 `cd web && npm run dev`（3000）。
2. 每轮代码修改完成并通过检查后，重启受影响的服务（后端是 tsx 无热重载，必须重启；前端 Next dev 热更新，一般无需重启），并确认可访问后再结束本轮。
