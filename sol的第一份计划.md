# 重写《sol的第一份计划.md》为挑战杯唯一技术执行总规

## 一、文档目标

完整清空并重写 [sol的第一份计划.md](</D:/23661/桌面/IM-Training-Agent/sol的第一份计划.md>)，使其成为另一个 Agent 唯一需要阅读的、可直接执行的技术开发总规。

文档必须：

- 逐条映射挑战杯 30 分完整性、25 分创新性、15 分体验、30 分实用价值评分要求。
- 明确区分“已经真实落地”“部分落地”“尚未实现”，禁止把设计稿写成完成状态。
- 以 2026-09-05 为截止日期，采用“评分闭环优先、必要架构收口随后”的实施顺序。
- 锁定 Compose 为最终部署基线，PostgreSQL 为唯一业务数据源，Redis 只承担队列和事件。
- 保留现有路径、协同学习、学习资源三个工作台，只做比赛能力所需的局部增强。
- 记录当前未提交的头像、画像弹窗和演示画像改动，要求执行 Agent 保留、验证并收口，不得覆盖。
- 移除旧文档中的 `:codex-annotation`、过时状态、旧日期计划和与代码不符的描述。

## 二、技术总规内容

### 1. 现状审计与评分差距

写入基于代码的真实基线：

- 已有 BullMQ StudyRun、动态 DAG、SSE、Claim 审核、修订门禁、六类资源、BKT 更新、12 题诊断后端、三种画像种子、60 案例定义。
- 当前根类型检查、115 个测试和前端生产构建通过。
- 身份、画像、路径、资源和证据仍主要依赖 SQLite，StudyRun 使用 PostgreSQL，属于双数据源过渡状态。
- Compose 缺少 Web 和数据库初始化服务，API/Worker 镜像也没有完整的数据启动链路。
- 配置的 PostgreSQL、Redis 和 Docker 当前不可用，前后端及 Worker 未运行。
- 诊断没有前端流程；苏格拉底导学没有完整实现；画像缺知识盲区、资源匹配与难度曲线。
- PostgreSQL 向量已离线回填，但运行时混合检索和 RRF 尚未落地。
- 当前离线评测只验证知识库存在性和公式结果，不能作为幻觉率或真实生成效果的最终证据。
- 现有“辩论”主要是规则化质询，需升级为独立批评 Agent、裁决 Agent 与确定性发布门禁共同工作的可信协作。

### 2. 三项可证明的核心创新

文档锁定以下创新，所有创新必须有代码、运行记录、消融实验和可导出证据，不能只写概念：

- **风险自适应协作 DAG**：根据画像置信度、近期错误率、先修缺口和证据覆盖动态选择节点、风险等级、质询重点和修订次数；自定义角色不能移除 Claim 审核、裁决和隐私门禁。
- **声明—证据—争议裁决图**：生成内容拆为原子 Claim，每条绑定 Evidence；独立批评 Agent 检测无证据、证据冲突、越界因果、数字不一致和难度错配；裁决 Agent 输出结构化判决；代码门禁决定发布、修订或人工复核。不得展示模型隐式思维链，只展示公开结论和证据。
- **诊断—BKT—难度—追问双闭环**：学历和自述只作先验，诊断与练习更新 BKT；资源难度由掌握概率、置信度和先修就绪度计算；低置信关键知识点进入最多五轮苏格拉底追问；反馈再次更新画像、路径建议和下一次 DAG。

增加三组消融实验：

- 动态 DAG 对比固定 DAG。
- Claim 辩论裁决对比仅生成或仅一次审核。
- BKT 难度校准对比固定难度。

### 3. 目标架构与数据流

锁定最终拓扑：

```text
Web
  → Express API
  → PostgreSQL 16 + pgvector（唯一业务状态）
  → Redis/BullMQ（任务、锁、事件）
  → Worker（DAG 节点执行）
  → DashScope 生成与嵌入模型
```

实施要求：

- 建立 PostgreSQL Repository 层，替代运行时 `IdentityStore`、`LearningStore` 和 `EvidenceService` 的 SQLite 依赖；禁止长期双写。
- SQLite 只作为只读迁移源和可恢复备份。
- 增加 Compose 的 `migrate/bootstrap` 与 `web` 服务；空数据卷启动时自动应用迁移、导入诊断题、知识切片和仓库内 AI4I 数据。
- MetroPT-3 完整 CSV 通过外部数据卷可选挂载；未安装时明确显示数据状态，不生成伪数据。
- 文档检索采用 PostgreSQL FTS + pgvector 双路召回，RRF 合并 top-8；嵌入失败降级为 FTS，并把降级写入 Run 事件。
- 临时上传正文只存在当前 Run 内存/队列生命周期中，数据库仅保留哈希、大小、文件名、使用范围和删除结论。

### 4. 产品闭环

依次实施：

1. 注册、建档后强制进入 12 题诊断；演示种子账号可预置诊断结果。
2. 诊断更新 BKT，并据此生成第一棵差异化学习路径。
3. 画像弹窗展示知识盲区、掌握概率、置信度、证据数量、难度匹配曲线和当前建议。
4. 学习任务生成风险自适应 DAG，通过 SSE 展示可公开的节点状态、证据、质询和裁决。
5. 六类资源统一经过证据、结构、Claim、辩论、裁决和隐私门禁。
6. 资源阅读、笔记、掌握反馈和分层习题更新 BKT。
7. 系统生成“补强、保持、进阶、启发式追问”决策；用户节点完成状态仍只能由用户手动修改。
8. 低置信关键节点进入苏格拉底会话，每轮一个问题，最多五轮；终止后生成资源或更新路径建议。
9. 每次 Run 可以导出画像、诊断、DAG、EvidencePack、Claim 图、裁决、资源、反馈和画像变化。

## 三、接口与数据契约

保留并完善现有 Runs、诊断、资源和画像接口，新增或扩展：

- `GET /api/auth/me`：返回 `diagnosticCompleted`，供前端决定是否进入诊断。
- `GET /api/learning/diagnostic`、`POST /api/learning/diagnostic-attempts`：保持服务端判分，禁止提前下发答案。
- `GET /api/learning/profile`：增加 `blindSpots`、`difficultyCurve`、`resourceMatch`、`latestDiagnostic` 和可解释依据。
- `POST /api/learning/guidance/sessions`：按路径节点创建苏格拉底会话并返回首题。
- `POST /api/learning/guidance/sessions/:sessionId/answers`：提交回答，返回公开评价、BKT 变化、下一题或终态决策。
- `POST /api/learning/runs`：增加幂等键；规划信号必须来自持久化 BKT、先修状态和历史作答。
- `GET /api/learning/runs/:runId/events`：保持单调序号、断线续传和公开摘要。
- `POST /api/learning/runs/:runId/cancel`：保持幂等取消。
- `GET /api/learning/runs/:runId/export`：补充运行前后画像快照、检索降级状态和声明—证据边。
- 路径页的对话接口暂时保留；只有迁移到新的路径决策接口并确认前端零调用后才能删除。

新增 PostgreSQL 实体：

- `guidance_sessions`、`guidance_turns`：保存追问轮次、评分和 BKT 前后状态。
- Run 请求幂等键唯一约束。
- Claim 增加声明类型、来源范围、批评结论和最终裁决。
- 画像快照保留生成依据版本，以支持前后对比和比赛导出。

## 四、三账号与评测设计

最终必须使用 3 个账号，而不是 2 个：

- `learner-foundation`：中职机电、Python 零基础，路径重基础、资源脚手架强。
- `learner-advanced`：自动化本科、会 MATLAB/基础 Python，快速进入时序分析与算法比较。
- `learner-maintenance`：在职运维、时间有限，侧重传感器语义、现场动作和诊断报告。

`demo:seed` 必须幂等生成建档、诊断、BKT、路径和画像；密码只从环境变量读取。三账号分别导出完整输入、协同中间过程和最终资源，满足提交最低 2 组及满分档 3 组要求。

评测改为 60 个可恢复的真实端到端案例：

- 三画像各 20 个，覆盖 AI4I、MetroPT-3、六类资源和基础/进阶/迁移任务。
- 每个案例保存黄金知识点、允许证据、目标难度、预期结构和禁止声明。
- 区分“生成初稿幻觉率”和“发布资源幻觉率”，证明门禁带来的实际下降。
- 发布资源幻觉率 `<5%`，难度适配准确率 `≥85%`，核心知识覆盖率 `≥90%`。
- `review`、无 Evidence 或超出允许证据范围的 Claim 不得通过发布门禁。
- 至少抽取 15 个分层案例及全部失败案例进行双人复核，保存复核记录。
- 输出总报告、逐案例报告、消融实验和三账号差异对比；任何核心阈值未达标不得标记完成。

## 五、执行顺序与完成门禁

### 阶段一：基线与可部署性

- 保护当前未提交改动，完成头像和画像弹窗验证。
- 修复 Compose 的 PostgreSQL、Redis、初始化、API、Worker、Web 全链路。
- 完成 PostgreSQL 单数据源切换和干净环境初始化。
- 验收：空卷 Compose 启动、注册登录、知识检索和 Run 全部可用。

### 阶段二：真实个性化

- 接入诊断前端、BKT 审计、画像盲区和难度曲线。
- 实现苏格拉底会话及反馈决策。
- 验收：三种画像在相同任务下产生可解释的不同路径、难度和下一步动作。

### 阶段三：可信多智能体创新

- 落地运行时混合检索、独立批评、裁决、修订和发布门禁。
- 完善 SSE 展示与证据图导出。
- 验收：无证据、错误数值、冲突证据和越界因果均被拦截；Worker 重启可恢复。

### 阶段四：资源与体验闭环

- 完成六类资源结构化渲染和统一门禁。
- 串通阅读、作答、反馈、BKT、路径建议和下一轮任务。
- 验收：完整用户旅程无需手工改数据库或刷新恢复状态。

### 阶段五：评测与交付

- 完成三账号、60 个真实案例、消融实验、证据包和 10 分钟演示脚本。
- 冻结代码后只修阻断问题。
- 验收：指标达标、三账号隔离、Compose 干净部署、测试与构建全绿。

## 六、测试和最终验收

必须覆盖：

- 动态 DAG 依赖闭合、强制门禁、幂等、取消、重试、恢复和 SSE 续传。
- Cookie 账号隔离，跨账号 Run、资源、画像、证据和导出均返回 404 或不可见。
- 诊断答案保密、BKT 数学更新、先修缺口、难度边界和苏格拉底终止条件。
- FTS/向量/RRF 排名、嵌入降级和 EvidencePack 定位。
- 独立批评与裁决结构校验、修订预算、失败不发布。
- 六类资源契约、习题判分、反馈回流和路径建议。
- Compose 空卷启动、健康检查、数据库迁移和服务重启恢复。
- 根类型检查、单元测试、服务端构建、Web 构建和核心浏览器端到端流程。

完成定义：

- 不允许以离线公式推演代替真实生成评测。
- 不允许以模型自评作为唯一准确性证据。
- 不允许把 Agent 名称或聊天动画当作创新证明。
- 不允许存在需要 SQLite 才能运行的生产链路。
- 不允许在未达到三项比赛指标时写“开发完成”。
- 最终提交物包含源码、Compose 部署说明、测试数据、三账号证据包、评测报告、消融报告、作品设计实现方案、PPT 和不超过 10 分钟的演示视频。

## 七、文档替换验证

重写后只修改目标 Markdown 文件，并检查：

- 文件不再包含旧内容或 `:codex-annotation` 标记。
- 赛题四项评分均有“实现—证据—验收”对应关系。
- 所有“已完成”陈述均能在当前代码中找到依据。
- 所有待实施项都有依赖、接口、数据流、验收命令和失败处理。
- 明确 3 个账号、60 个真实案例、三项指标和 Compose 主线。
- 不创建带 v1/v2 名称的替代计划文件。
- 不暂存或覆盖当前工作区中其他未提交代码。
