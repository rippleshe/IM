# IM-Training-Agent 挑战杯技术开发总规计划

## 总体目标

- 新建一份单一、可直接交给开发 Agent 执行的技术总规：`docs/挑战杯技术开发总规.md`。文档包含现状审计、目标架构、接口、数据模型、算法、迁移、任务顺序、测试指标、部署与验收，不写成项目介绍或申报书。
- 以 2026-09-05 为硬截止线：先完成挑战杯评分闭环，同时采用可长期演进的平台架构，不制造临时 v1/v2 文件或重复实现。
- 核心创新确定为“双核心闭环”：
  1. 动态多智能体 DAG、反方质询、证据声明图与裁决门禁，解决幻觉和可追溯性。
  2. 初始诊断、知识状态追踪、难度校准和苏格拉底提问，解决真实个性化。
- 当前 3 个用户中有 2 个是同质化验证账号，不能作为差异化案例。最终固定建设 3 个真实隔离演示账号，而不是只做 2 个。

## 核心实施设计

### 1. 平台基础设施与数据迁移

- 使用 Docker Desktop 和 Compose 启动 PostgreSQL 16 + pgvector、Redis 7、API、BullMQ Worker；PostgreSQL 是唯一业务数据源，Redis只负责队列、锁和实时事件。
- Node/TypeScript 全栈实施，统一使用 pnpm；数据库采用 `pg + drizzle-orm` 管理结构迁移，BullMQ 管理异步任务。
- 知识检索采用 PostgreSQL 全文检索与 pgvector 混合召回，通过 RRF 合并排序；默认嵌入模型为 `text-embedding-v4`、固定 1024 维。该模型支持 OpenAI-compatible 调用及自定义维度，[阿里云官方规格](https://help.aliyun.com/zh/model-studio/text-embedding-v4)。
- 编写幂等 SQLite→PostgreSQL 迁移程序：源库只读、分批 COPY、事务提交、表级行数校验、CSV SHA256 校验、失败回滚和迁移报告。完成校验后才允许切换运行数据源，原 SQLite 仅作为可恢复备份。
- MetroPT-3 当前已真实导入 1,516,948 行；干净环境必须支持“官方下载—SHA256 校验—解压—批量导入—行数和时间边界核验”，无原始 CSV 时明确显示数据准备状态，不生成伪时序数据。:codex-annotation{index="3"}
- 保持 `web/src/app/page.tsx` 为轻量产品入口，已删除的旧通用工作台、无引用包袱和 `@ts-nocheck` 不得重新引入；新增能力进入现有路径、学习、资源三个工作台。:codex-annotation{index="1"}

### 2. 动态多智能体可信协作

- 建立持久化 `StudyRun` DAG，由以下职责节点组成：学情建模、证据检索、领域分析、资源生成、反方质询、证据裁决、隐私合规；一次运行至少执行 3 个业务角色，审核和合规属于不可跳过门禁。
- “自动编排”根据资源类型、画像不确定度、知识风险和证据覆盖度生成 DAG；“指定角色”中的 `selectedAgentIds` 必须真实约束执行计划，但不能取消必要门禁。修复当前前端可选择、后端却忽略角色的假交互。
- BullMQ 节点任务采用 `runId + nodeKey + attempt` 幂等键，失败最多重试 2 次；支持取消、超时、断点恢复。PostgreSQL 持久化最终状态，Redis 只分发事件。
- 每项可核验事实拆成 `Claim`，必须关联 EvidencePack 中的证据；数值、时间范围和故障窗口由确定性工具核验。
- 资源生成后进入“主张—质询—裁决”流程：反方指出无证据、证据冲突、越界因果和难度不适配；裁决节点给出 `supported / partial / conflict / unsupported`。最多修订 2 次，仍不合格则禁止发布并进入人工复核。
- 前端展示角色、任务节点、状态、证据、质询结论和裁决依据，不展示模型隐式思维链。

### 3. 学情建模与个性化算法

- 入门流程加入 12 道诊断题，覆盖 Python、数据处理、统计、时序和设备诊断；自述只作为先验，不能直接当作掌握度。
- 每个知识点维护 BKT 状态：掌握概率、猜测率、失误率、学习转移率和置信度；诊断、作答、阅读反馈和任务完成均产生可审计状态更新。
- 资源难度不再硬编码 `0.42`。根据知识点掌握度、先修缺口、题目难度和脚手架强度计算 `targetDifficulty`，将预计成功率控制在 65%–80% 的有效学习区间。
- 苏格拉底提问优先选择“路径关键且掌握置信度最低”的知识点；每轮只问一个问题，回答后更新状态。置信度达到 0.80 或完成 5 轮后结束诊断并生成资源。
- 画像页面展示知识盲区、掌握置信度、资源难度匹配曲线和动态学习路径；学习反馈低时降低抽象维度并补先修，高时生成迁移题或挑战任务。
- 六类资源统一实现：讲义、分层习题、实操指南、知识图谱、复习卡、挑战任务。分层习题固定 L1/L2/L3 各 3 题；每题带知识点、答案、解析、难度和证据引用。知识图谱使用 Mermaid 渲染。

### 4. 三个差异化账号

- `learner-foundation`：中职机电背景、Python 零基础，目标为理解数据并完成基础诊断。
- `learner-advanced`：自动化本科、具备 MATLAB/基础 Python，目标为时序异常检测与算法比较。
- `learner-maintenance`：在职设备运维、每周时间有限，目标为形成可执行诊断报告。
- 使用 `pnpm demo:seed` 幂等创建账号、诊断结果、学习路径和起始状态；演示密码只能由 `IM_TRAINING_AGENT_DEMO_PASSWORD` 注入，不提交到仓库。
- 每个账号可导出完整证据包：初始画像、诊断作答、DAG 节点事件、Agent 结论、EvidencePack、最终资源、学习反馈和画像变化。

## 公共接口、类型与数据模型

### API

- `POST /api/learning/runs`：创建协同学习运行，返回 `202 + runId`。
- `GET /api/learning/runs/:runId`：读取运行快照、DAG、节点状态和最终资产。
- `GET /api/learning/runs/:runId/events`：使用 SSE 和 `Last-Event-ID` 推送增量运行事件。
- `POST /api/learning/runs/:runId/cancel`：取消尚未结束的运行。
- `GET /api/learning/diagnostic`、`POST /api/learning/diagnostic-attempts`：获取诊断题并提交一次完整诊断。
- `GET /api/learning/profile`：扩展返回知识掌握度、置信度、盲区和难度曲线。
- `GET /api/learning/runs/:runId/export`：导出比赛证据包。
- 现有资源反馈和作答接口保留，但写入 BKT 更新事件；旧 `/api/learning/study/chat` 在前端切换完成后同批删除，不长期维持两套入口。
- 所有接口和 SSE 都从 HttpOnly Cookie 服务端会话推导 `learnerId`，请求体、队列消息和 URL 中的用户标识均不得作为授权依据；不恢复通用 `/api/sessions/*` 或 WebSocket 链路。:codex-annotation{index="2"}

### 核心类型

- `StudyRunRequest`：任务、路径节点、资源类型、协作模式、偏好角色、临时材料。
- `StudyRunPlan`：DAG 节点、依赖、必要门禁、风险等级、资源预算。
- `RunEvent`：单调递增序号、运行/节点、事件类型、公开摘要、时间戳。
- `LearnerKnowledgeState`：知识点、掌握概率、置信度、证据来源、更新时间。
- `DifficultyCalibration`：目标难度、预计成功率、依据和实际反馈。
- `Claim`、`ClaimEvidenceEdge`、`DebateIssue`、`AuditDecision`：构成可查询证据声明图。
- `EvaluationCase`：学习者画像、任务、必备知识点、目标难度区间、允许证据和预期资源结构。

### PostgreSQL 领域表

按身份与会话、画像与诊断、知识状态与路径、资源与反馈、领域知识与向量、EvidencePack、运行 DAG 与事件、辩论与声明图、评测报告九组建表。所有学习数据表必须包含 `learner_id` 并执行服务层隔离；临时上传只保存哈希、大小、有效期和审计结果，正文不落库。

## 测试、量化验收与排期

### 量化评测

- 建立 60 个固定评测案例，三个画像各 20 个，覆盖 AI4I、MetroPT-3、六类资源及基础/进阶/迁移任务。
- 幻觉率：`unsupported factual claims / auditable factual claims`，要求 `<5%`。
- 难度适配准确率：资源难度区间、脚手架要求和目标画像全部匹配的案例数占比，要求 `≥85%`。
- 核心知识覆盖率：已生成且有证据支持的必备知识点数占黄金知识点数，要求 `≥90%`。
- 自动跑完 60 个案例；对 12 个分层样本及全部失败案例人工复核。任一挑战杯核心阈值未达到，都不能标记开发完成。

### 必测场景

- 三账号跨会话、资产、SSE、队列和导出数据完全隔离。
- DAG 依赖、指定角色、强制门禁、重试、取消、断点恢复和重复请求幂等。
- 无证据主张、冲突证据、错误数值、越界因果均被拦截。
- 诊断前后画像变化、低掌握降阶、高掌握升阶、难度曲线更新。
- 六类资源结构、分层习题评分、Mermaid 渲染和反馈回写。
- SQLite 完整迁移、Metro 1,516,948 行核验、干净环境重新导入。
- 保持现有 95 项测试全绿，并增加评分关键链路的单元、集成和端到端测试。

### 截止线安排

- 8 月 28–29 日：Docker/数据库/Redis、数据模型、迁移和运行事件协议。
- 8 月 30–31 日：BullMQ 动态 DAG、真实角色选择、证据声明图、质询与裁决。
- 9 月 1 日：诊断、BKT、难度校准和苏格拉底提问。
- 9 月 2 日：六类资源、画像可视化、SSE 协作展示。
- 9 月 3 日：三个账号、60 案例评测、导出证据包。
- 9 月 4 日：部署验证、数据隔离、指标复核和演示材料接口。
- 9 月 5 日：只处理阻断问题并保留提交缓冲。

## 已锁定假设

- 允许安装 Docker Desktop并立即引入 PostgreSQL/pgvector 与 Redis/BullMQ。
- 现有 SQLite 数据全部迁移，不采用长期双数据库运行。
- 默认生成模型继续使用 `qwen-plus`；嵌入使用 `text-embedding-v4` 1024 维，密钥仅存于本机环境。
- 前端遵循项目保护原则，只做支撑真实业务链路的局部增强。
- 长期扩展通过数据库适配器、事件协议和 Worker 横向扩展实现；挑战杯硬门禁未通过前，不投入多租户、复杂权限或无关平台功能。
